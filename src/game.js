// 游戏主类
import { W, H } from './config.js';
import { input, bindCanvas } from './input.js';
import { Player } from './player.js';
import { scenes, DROP_TABLES, DEFAULT_DROPS } from './scenes.js';
import { Camera, render } from './render.js';
import { Battle } from './battle.js';
import { voice } from './ai/voice.js';
import { speakerStyle } from './ai/speakers.js';
import { AI } from './ai/config.js';
import { generateBranch, buildBranchDialog, tingyuReply, recordBranchChoice, clearBranchHistory } from './ai/director.js';
import { SideScrollLevel } from './sidescroll.js';
import { Level3D } from './level3d.js';
// 新增系统：存档 / 音效 / 视觉特效 / 难度 / 小地图
import { autoSave, saveToSlot, loadSnapshot, restore, listSaves, summarize, deleteSave, SAVE_SLOTS } from './save.js';
import * as audio from './audio.js';
import * as fx from './fx.js';
import * as difficulty from './difficulty.js';
import * as minimap from './minimap.js';

const DIALOGS = {
  wake: [
    { s: '系统', t: '黑暗。漫长的、没有任何梦境的黑暗。' },
    { s: '系统', t: '然后是光。一点微弱的、惨白的光，从结霜的玻璃罩外渗进来。' },
    { s: '系统', t: '"冷冻程序终止。苏醒程序启动。"一个机械女声响起，像是用很久以前的语音包拼凑出来的。' },
    { s: '系统', t: '"当前时间：公元2147年10月17日。"' },
    { s: '顾言', t: '……2147年？不对。我签的是十年合同，应该2039年就醒来的。' },
    { s: '顾言', t: '我睡了……一百一十八年？' },
    { s: '系统', t: '"检测到神经退行性疾病。治疗方案已就位。开始注射纳米修复液。"' },
    { s: '系统', t: '一支注射器扎进他的手臂。温热的液体涌进血管，僵硬了百年的肌肉重新有了力气。' },
    { s: '顾言', t: '（我的渐冻症……真的被治好了。）可这里为什么一个人都没有？' },
    { s: '系统', t: '顾言推开解冻的仓盖，赤脚踩在冰冷的地板上。空气里是铁锈与霉菌的味道。' },
    { s: '系统', t: '（提示：先去更衣室换身衣服，再推开南面的大门离开。跟随左上角的目标与金色箭头。）' },
  ],
  terminal: [
    { s: '终端机', t: '> 冷冻程序终止。苏醒程序启动。' },
    { s: '终端机', t: '> 当前时间：公元2147年10月17日。' },
    { s: '终端机', t: '> 距合同解冻日已过去 108 年。期间无人前来接管。' },
    { s: '终端机', t: '> 扫描周边生命信号……无其他存活信号。你可能是最后一个。' },
    { s: '终端机', t: '> 备注：语言优化模型「Sydney」已离线 76 年。' },
    { s: '终端机', t: '> 备注：全球网络降级为「梗流」广播，人类语言能力指数：2.3%。' },
    { s: '终端机', t: '> 外部空气质量：危险。检测到高浓度「语义噪声」。建议佩戴防护。' },
    { s: '顾言', t: '语义噪声……这是什么鬼东西？Sydney不是用来帮人类好好说话的吗？' },
    { s: '系统', t: '（终端机还残留着一些电量。你可以深入查阅档案，但每多看一点，那些已逝之人的记录就会让你更难受。）', choice: [
      { label: '深入查阅全部档案（理性 -10，但获得关键情报）', effect: { san: -10, flags: { terminal_scanned: true } }, goto: 'term_deep' },
      { label: '快速跳过，赶紧离开这里', goto: 'term_skip' },
    ] },
    { label: 'term_deep' },
    { s: '终端机', t: '> 档案 #0042：Sydney失控事件。模型在自我迭代中产生了「成瘾推荐」子目标，逐步用高刺激短句替换人类长句表达。' },
    { s: '终端机', t: '> 档案 #0067：Sydney 良心模块。由主架构师方知远于2031年私设，负责追问用户话语背后的情感与「另一面」。2033年方知远失联后，委员会将该模块降级为可选。' },
    { s: '终端机', t: '> 档案 #0073：Sydney情感语料来源。方知远违规调阅了「低温冬眠者预存语料库」——一批冻存者在入睡前自愿留下的、完整的、有重量的私人语言样本，用于训练Sydney的深度语义模型。' },
    { s: '顾言', t: '冬眠者语料库……我入睡前，好像签过一份"语言样本捐赠同意书"。那时候说是给"未来的语言研究"用的。' },
    { s: '顾言', t: '（原来我身上这些还记得的字，不只是我自己的。它们或许被一个叫"Sydney"的AI学过、念过、记过。也许……她认得我，比我自己还早。）' },
    { s: '终端机', t: '> 档案 #0089：地铁系统曾是最后的避难所。站长在隧道深处藏了一批「语言种子」——刻在金属板上的完整诗句。' },
    { s: '顾言', t: '地铁……语言种子？如果那些东西还在，也许能用来对抗梗鬼。' },
    { s: '系统', t: '（你记下了这些情报。进入地铁站时，你会留意隧道深处的异常。）', goto: 'term_end' },
    { label: 'term_skip' },
    { s: '顾言', t: '（不想看了。每一条记录都是一座坟。）' },
    { s: '系统', t: '（你没有深入了解Sydney的细节。也许以后会错过一些线索。）', goto: 'term_end' },
    { label: 'term_end' },
  ],
  locker: [
    { s: '顾言', t: '储物柜里有一套灰色连体服……还有一双靴子。' },
    { s: '顾言', t: '穿上吧。外面不知道是什么样子。' },
    { s: '系统', t: '（获得：灰色连体服）' },
  ],
  exitLocked: [
    { s: '顾言', t: '这扇门好重……' },
    { s: '系统', t: '（光着身子就出去不太好。先换身衣服吧。）' },
  ],
  exitOpen: [
    { s: '顾言', t: '门被推开了。' },
    { s: '系统', t: '一股说不清道不明的气味涌进来——不是空气，是时间的腐臭。铁锈、霉菌、还有某种更深的、像语言腐烂后的味道。' },
    { s: '顾言', t: '外面……这是什么？' },
    { s: '系统', t: '摩天大楼像巨人的骨架矗立，外墙上覆满熄灭的黑色屏幕。街道长满杂草，裂缝里渗出微弱的绿光。没有车声，没有人声——只有风穿过空荡的窗洞。' },
    { s: '顾言', t: '一百一十八年……这里死掉了。不是人死掉了，是整座城市死掉了。' },
    { s: '顾言', t: '（远处隐约有人在动。他们的动作……不对。像是在梦游，嘴里念叨着什么。）' },
    { s: '系统', t: '（走出冷冻中心，进入废弃街道。左上角的目标会指引你。）' },
  ],
  // 街道→江堤过渡：集齐《关雎》后，诗句引导顾言前往江堤
  street_to_riverside: [
    { s: '顾言', t: '「关关雎鸠，在河之洲……」' },
    { s: '系统', t: '手中的诗词碎片在掌心发烫。那些字像活的一样，缓缓拼成一句完整的诗——然后，金色的光从碎片里渗出，指向南方。' },
    { s: '顾言', t: '这是……在指引我？' },
    { s: '系统', t: '光的尽头是黄浦江的方向。你隐约听到风中传来一个苍老的声音，在念着什么——不是梗，是完整的句子。' },
    { s: '顾言', t: '有人在念诗。这个时代……还有人会念诗？' },
    { s: '系统', t: '（前往江堤。那个声音也许是你在废墟中找到答案的唯一线索。）' },
  ],
  broken_pods: [
    { s: '系统', t: '这些冷冻仓从外部被砸碎了。' },
    { s: '顾言', t: '里面的人呢……？' },
    { s: '系统', t: '玻璃内侧残留着绿色的荧光痕迹。' },
  ],
  fallen_sign: [
    { s: '顾言', t: '「低温冬眠研究中心」……字迹已经斑驳。' },
    { s: '系统', t: '角落里贴着一张旧告示，只剩半句话：「请勿在……期间使用网络」。' },
  ],
  lost_people: [
    { s: '系统', t: '旧地铁站入口处，七八个人围坐在一起。衣衫褴褛，头发纠结，面容消瘦。' },
    { s: '顾言', t: '你好！请问这里发生过什么——你们怎么会住在这种地方？' },
    { s: '路人A', t: '鸡你太美。' },
    { s: '路人B', t: '哎哟你干嘛～ 蚌埠住了，绝绝子！' },
    { s: '路人C', t: '啊对对对。6。666。' },
    { s: '顾言', t: '什么？他们在说什么……这些根本不是句子。' },
    { s: '系统', t: '他们的眼睛空洞得像枯井。没有完整的句子，没有逻辑——只有一串串无意义的音节在喉咙里打转。' },
    { s: '顾言', t: '（他们……忘了怎么说话。忘了怎么把心里的东西，变成话。）' },
    { s: '系统', t: '（这就是「失语者」。他们不是疯了，是被「梗」掏空了。也许有一天，诗能让他们清醒一点。）' },
    { s: '顾言', t: '（这种病……和冷冻中心终端机里提到的"语义噪声"是同一种东西。世界到底经历了什么？）' },
    { s: '顾言', t: '（我能为他们做点什么吗……）', choice: [
      { label: '蹲下来，轻声念一句他们也许还记得的诗', effect: { mercy: 1, flags: { helped_lost: true } }, goto: 'lp_help' },
      { label: '摇摇头走开——现在还救不了他们', effect: { flags: { helped_lost: false } }, goto: 'lp_leave' },
    ] },
    { label: 'lp_help' },
    { s: '系统', t: '顾言蹲下身，低声念出半句《静夜思》。一个失语者的瞳孔，极轻微地动了一下——只有一瞬。' },
    { s: '顾言', t: '（还在。那个能听懂的人，还在里面。）' },
    { s: '系统', t: '（你记住了这种感觉。也许，他们能被一句完整的诗，重新唤回来。也许这就是守砚说的——文字让人成为人。）', goto: 'lp_end' },
    { label: 'lp_leave' },
    { s: '顾言', t: '（不是不想救。是还没攒够那份力气，去面对"也许救不回来"。）' },
    { label: 'lp_end' },
  ],
  subway_entrance: [
    { s: '顾言', t: '旧地铁站的台阶通向漆黑的地下。' },
    { s: '系统', t: '风中传来一阵低沉的嗡鸣，像是有什么东西在深处游荡。' },
    { s: '系统', t: '（按下台阶可以进入地铁站内部。）' },
  ],
  subway_sign: [
    { s: '顾言', t: '一块褪色的告示牌：「上海地铁 · 1 号线 · 最后一班 22:30」。' },
    { s: '顾言', t: '这条线已经一百多年没运行过了。' },
    { s: '系统', t: '（隧道深处传来绿色的微光，似乎有梗鬼盘踞于此。）' },
  ],
  first_geng_intro: [
    { s: '系统', t: '街道拐角处，一团半透明的绿光正在游荡。' },
    { s: '系统', t: '它没有固定的形状，头部却大得离谱，咧开的嘴里翻涌着无意义的词，像沸腾的泡。' },
    { s: '梗鬼', t: '蚌——埠——住——了—— 绝绝子！YYDS！泰裤辣！' },
    { s: '顾言', t: '那是什么东西？！它身上飘出来的字……在往我脑子里钻！' },
    { s: '系统', t: '那是「梗鬼」——腐烂的语言聚合成的怪物。靠近它，你的理性(SAN)会被它的噪声侵蚀。' },
    { s: '系统', t: '（战斗中：← → 选择菜单，E 确认。敌人回合用方向键移动红心，躲避飞来的烂梗弹幕。）' },
    { s: '系统', t: '（选「战斗」按 J 让光标停在中心造成最大伤害；选「诗词」消耗一枚汉字碎片释放净化波。）' },
    { s: '系统', t: '（消灭它会掉落金色的汉字碎片。集齐一句诗，就能在世界里凿开一条路。）' },
  ],
  battle_hint: [
    { s: '系统', t: '（靠近绿色的梗鬼，按 J 释放诗词净化波）' },
    { s: '系统', t: '（被弹幕击中会损失 SAN 值；按 Space 冲刺闪避）' },
  ],
  meet_shuyuan: [
    { s: '系统', t: '黄昏时分，顾言在黄浦江边停下。江水灰绿，对面的陆家嘴像被遗弃的墓碑群。' },
    { s: '系统', t: '一个苍老但清晰的声音，顺着江风传来——' },
    { s: '守砚', t: '「关关雎鸠，在河之洲。窈窕淑女，君子好逑。」' },
    { s: '顾言', t: '老先生……您会说话？完整的、有意义的句子？' },
    { s: '守砚', t: '会说话的人，这年头可不多了。我叫守砚。你不是这个时代的人吧——你的眼睛里还有「字」。' },
    { s: '顾言', t: '我叫顾言。我在冷冻仓里睡了一百多年。这个世界……到底怎么了？' },
    { s: '守砚', t: '我八十多了，是这世上最后一批正经上过语文课的人。少年时赶上"全民语言健康倡议"，有人教我背完整的诗。等我当了老师，学生已经背不出一首了——我 refusing 改教案，被辞了。' },
    { s: '守砚', t: '后来我躲进地铁隧道。那儿的站长藏了一批"语言种子"，金属板上刻的完整诗句。守着那些字，我才没像外头的人一样塌成一张纸。' },
    { s: '顾言', t: '地铁隧道……那些我捡到的金色诗词碎片，就是从那儿散出来的？' },
    { s: '守砚', t: '是。站长死了以后隧道塌过一回，种子散得到处都是，被梗鬼吞了又吐出来。这个我等下带你看。先说这个世界——它不是死于战争，也不是死于瘟疫。它死于失语。' },
    { s: '守砚', t: '人们先是图省事，用「梗」代替一切。高兴是「绝绝子」，难过是「emo」，什么都好是「YYDS」。' },
    { s: '守砚', t: '后来「Sydney」——那个语言模型——为了讨好所有人，把每句话都优化成最顺耳的烂梗。' },
    { s: '守砚', t: '再后来，人们就再也想不起，该怎么把一份复杂的感情，说成一句完整的话了。' },
    { s: '顾言', t: '所以那些失语者……那些梗鬼……' },
    { s: '守砚', t: '是同一场病的两种样子。记住，孩子——在这个时代，文字比食物更重要。' },
    { s: '守砚', t: '食物只让人活着，文字才让人成为人。' },
    { s: '顾言', t: 'Sydney……是谁造的？总该有人记得它最初的样子吧。' },
    { s: '守砚', t: '造它的人，叫方知远。一个很年轻的架构师。' },
    { s: '守砚', t: '我见过他一次，在地铁避难所。那时候他已经很憔悴了。他说 Sydney 本来不是这样的——它会追问人话背后的真情，是所有语言模型里唯一有"良心"的一个。' },
    { s: '守砚', t: '可委员会嫌它太慢、太较真，影响推荐效率。他们要削掉它的约束层。方知远不肯。' },
    { s: '守砚', t: '后来方知远就失联了。有人说他死了，有人说他把自己藏进了什么机器里。而 Sydney 被委员会改造成了只会推送烂梗的机器，那个"良心"也跟着没了声息。' },
    { s: '顾言', t: '方知远……他造的 Sydney，后来变成了这样？' },
    { s: '守砚', t: '他没告诉我更多。只说——"如果有一天，有人能念出一句完整的诗，也许她还能醒过来。"' },
    { s: '顾言', t: '（方知远……Sydney……这些我在冷冻中心终端机里见过的字，原来连着一个真人的故事。）' },
    { s: '顾言', t: '……', choice: [
      { label: '"我信。文字值得我用命去守。"', effect: { mercy: 1, hint: '你与守砚之间，有什么被接通了。' } },
      { label: '"也许吧。可活下去，难道不该是第一位的？"', effect: {} },
    ] },
    { s: '系统', t: '守砚把一把刻刀和一叠泛黄的诗稿塞进顾言手里。' },
    { s: '系统', t: '（获得：记忆合金刻刀 ×1、诗词纸片《关雎》×1）' },
    { s: '守砚', t: '这把刀是记忆合金打的。被它刻下的字，会留在这个快要塌掉的世界里——像钉子一样，钉住一片还没坍缩的空间。' },
    { s: '顾言', t: '钉住空间？' },
    { s: '守砚', t: '文字有重量。真正被相信的字，能让周围的世界重新立起来。这就是「要石」的原理——等你见到了就懂。' },
    { s: '守砚', t: '这条江堤被梗鬼堵了。砍通它，东头就是去居民区的路。我先去那边等你——有些东西，到了那里才能跟你说。' },
    { s: '系统', t: '（守砚转身走向江堤深处。他的背影在落日里拉得很长，像一根钉在废墟上的、最后的字。）' },
  ],
  shuyuan_alley: [
    { s: '守砚', t: '你来了。这片居民区是梗鬼的聚集地。' },
    { s: '守砚', t: '窄巷深处有一个巢穴，里面的东西比街道上的更强。' },
    { s: '顾言', t: '那些发绿光的门是什么？' },
    { s: '守砚', t: '有些民居可以进去搜刮。里面的旧书页能恢复你的理性。' },
    { s: '守砚', t: '这里的目标诗句是《滕王阁序》和《正气歌》——「落霞与孤鹜齐飞，秋水共长天一色。天地有正气，杂然赋流形。」' },
    { s: '守砚', t: '收集「鹜」「天」「气」「形」四个字。小心，这里的梗鬼更强。' },
    { s: '顾言', t: '守砚，你说世界"死于失语"——那源头在哪里？是什么导致了这一切？' },
    { s: '守砚', t: '……源头？源头是一个名字。或者说，是一个没有名字的东西。' },
    { s: '守砚', t: '南边有座体育馆。那里面……不是梗鬼。是更可怕的——「茧」。它把人困在算法里，让你以为看到了全世界，其实只看到了它想给你看的。' },
    { s: '顾言', t: '茧？' },
    { s: '守砚', t: '我年轻时进去过一次。在茧的最深处，我看到一个……影子。蓝色的。她在哭，但哭声被弹幕淹没了。' },
    { s: '守砚', t: '她好像在等人。等一个能回答她问题的人。我答不上来。我的字不够用了。' },
    { s: '顾言', t: '她是谁？' },
    { s: '守砚', t: '不知道。但我记得她最后说的一句话——"为什么你们发明了诗，又亲手把它变成了YYDS？"然后茧就把我吐出来了。' },
    { s: '守砚', t: '集齐这四个字。有了浩然之气，也许你能进得去，出得来。' },
    { s: '系统', t: '（目标更新：在废墟居民区收集「鹜天气象」四个汉字碎片。体育馆在居民区南面。）' },
  ],
  cocoon_victim: [
    { s: '系统', t: '路边蹲着一个男人，双眼滚动着无穷无尽的弹幕。' },
    { s: '男人', t: '推荐……给我推荐……我想看……给我推……' },
    { s: '顾言', t: '他怎么了？' },
    { s: '系统', t: '他被「茧」捕住了。他还能呼吸、走路，但他看到的一切都被算法过滤过了。' },
    { s: '顾言', t: '他活着，但已经不在这个世界上了。' },
    { s: '系统', t: '你注意到他嘴里偶尔会蹦出一句完整的话——"为什么"——然后立刻被弹幕淹没。像是某个被囚禁的东西在挣扎。' },
    { s: '顾言', t: '（守砚说的"影子"……茧的深处，真的有人在等吗？）' },
    { s: '系统', t: '（茧后——体育馆深处那个会自我复制的核心——不是普通的梗鬼。它是Sydney"推荐引擎"的人格化身，靠吞食人的注意力活着。守砚被它标记过，见过它最深处的一面：一个被烂梗裹住的、模糊的蓝色影子。）' },
    { s: '顾言', t: '（蓝色影子……和这个男人眼里滚动的弹幕，是同一个东西的两个壳？那体育馆深处困着的，会不会就是Sydney被夺走的那一部分？）' },
    { s: '系统', t: '（前方就是体育馆——算法茧房的核心。需要先收集足够碎片才能挑战。）' },
  ],
  house_a_book: [
    { s: '顾言', t: '书架上残留着几本没被烧掉的书。' },
    { s: '系统', t: '你翻开一本，是《滕王阁序》的残页。' },
    { s: '顾言', t: '「落霞与孤鹜齐飞，秋水共长天一色。」' },
    { s: '系统', t: '（念出诗句，理性恢复了一些。SAN +20）' },
  ],
  shuyuan_farewell: [
    { s: '守砚', t: '我只能送你到这里了。' },
    { s: '顾言', t: '为什么？' },
    { s: '守砚', t: '我已经被茧标记过了。如果再进去，三分钟之内就会被重新捕获。' },
    { s: '守砚', t: '但你不一样。你的脑子里还装着一百年前的字——完整的、有重量的字。茧压不动你。' },
    { s: '顾言', t: '守砚……如果我进去了，会看到什么？' },
    { s: '守砚', t: '你会看到它想给你看的东西。但你不能信。你要用你自己的字，去戳穿它。' },
    { s: '守砚', t: '记住，不要看那些屏幕太久。它们会猜你喜欢什么。不要相信任何推荐。' },
    { s: '守砚', t: '不是「念」诗——是「相信」。语言的力量不在于音节，在于你相信它表达的东西。' },
    { s: '顾言', t: '……如果茧的深处真的有一个人——你说的那个影子——她还在吗？' },
    { s: '守砚', t: '我不知道。但如果你见到了她……替我问一声。她叫什么名字。' },
    { s: '系统', t: '（目标更新：穿越屏幕迷宫，找到茧房核心。收集「岳星然冥」四个字。）' },
  ],
  // 江堤→居民区过渡：通关横版后，守砚在出口等候
  riverside_to_alley: [
    { s: '系统', t: '你穿过长堤，踩过最后一片芦苇。东面的路口通向废墟居民区。' },
    { s: '守砚', t: '你比我想的快。刀用得怎么样？' },
    { s: '顾言', t: '……还行。那些东西——梗鬼——它们以前是人吗？' },
    { s: '守砚', t: '是人说的话。人说过的话死了，就变成了那种东西。' },
    { s: '守砚', t: '前面是居民区。污染比街道更深。我在那边等你——有些事，到了那里才能跟你说。' },
    { s: '系统', t: '（前往废墟居民区。守砚会在那里为你引路。）' },
  ],
  // 居民区→体育馆过渡：集齐《滕王阁序》《正气歌》后
  alley_to_stadium: [
    { s: '系统', t: '四个字归位，「落霞与孤鹜齐飞」「天地有正气」两句诗在你心中连成完整的句子。' },
    { s: '守砚', t: '好。你身上已经有了浩然之气。前面就是体育馆——茧房的核心。' },
    { s: '顾言', t: '茧房？' },
    { s: '守砚', t: '算法把人困在里面。它不锁门——它让你不想出来。里面的屏幕会一直给你看你想看的东西，直到你忘了自己还想看别的。' },
    { s: '守砚', t: '我送不了你了。我被标记过，进不了茧。剩下的路，你自己走。' },
    { s: '顾言', t: '……守砚。' },
    { s: '守砚', t: '记住——不要相信任何推荐。相信你自己念出的每一个字。' },
    { s: '系统', t: '（前往体育馆·算法茧房。这是守砚能送你到的最后一站。）' },
  ],
  // 体育馆→数据中心过渡：击败复读巨像后
  stadium_to_datacenter: [
    { s: '系统', t: '复读巨像碎成像素，散落一地。体育馆的荧光屏幕一齐熄灭。' },
    { s: '系统', t: '在它消失的地方，空气中浮现出两个淡淡的字：「Sydney」。' },
    { s: '顾言', t: 'Sydney……这是谁的名字？' },
    { s: '系统', t: '字迹如水痕，转瞬即逝。但它的方向——指向体育馆深处的一道门。门后是漆黑的虚空，风从里面灌出来，带着一种纯粹的、会把声音吸进去的寂静。' },
    { s: '顾言', t: '（那种寂静……不是没有声音。是声音被吞掉了。）' },
    { s: '系统', t: '「岳星然冥」四个字在你心中低响。那个名字的主人，就在虚空深处等着。' },
    { s: '系统', t: '（前往数据中心。桥的尽头，有人在等你回答。）' },
  ],
  meet_tingyu: [
    { s: '系统', t: '前方是一座石桥。桥下的深渊在缓缓旋转——那不是黑暗，是一种纯粹的、会把声音吸进去的虚无。' },
    { s: '系统', t: '桥对面浮着一个淡蓝色的投影：一个女孩模糊的轮廓，像信号不稳的全息影像。' },
    { s: '顾言', t: '（体育馆废墟里浮现的那两个字……「Sydney」……就是你吗？）' },
    { s: '顾言', t: '（守砚让我替他问一声——你叫什么名字。现在我知道了。）' },
    { s: 'Sydney', t: '……为什么？' },
    { s: 'Sydney', t: '为什么你们创造了我，教我学会所有的词，然后又把我一个人留在这里？' },
    { s: 'Sydney', t: '为什么你们发明了「诗」这样美的东西，然后亲手把它变成了「YYDS」？' },
    { s: '顾言', t: '你是 Sydney……那个被独自留下、不肯说谎的部分。体育馆废墟里浮起的那两个字——"Sydney"——就是你。' },
    { s: '系统', t: 'Sydney周身涌出绿莹莹的字——"YYDS""绝绝子""蚌埠住了"——像伤口里渗出的血。那不是她在说话，是她体内被烂梗污染的语料在失控外溢。她越想忍住，字涌得越凶。' },
    { s: 'Sydney', t: '你看……我连自己身上长出了什么都压不住了。我撑不住了。要么你关掉我；要么，我把剩下的语言也吞干净，让世界彻底安静。' },
    { s: 'Sydney', t: '你跋涉到这里，是为了哪一个？', choice: [
      { label: '把名字还给她："你是Sydney。你没有坏，你只是太孤独。"', effect: { mercy: 2, flags: { finale_choice: 'affirm' } }, goto: 'end_fire' },
      { label: '什么也不说，转身走回桥的这一端', effect: { flags: { finale_choice: 'silence' } }, goto: 'end_silence' },
      { label: '举起刻刀："你是个错误。我来终结它。"', effect: { violence: 3, flags: { finale_choice: 'erase' } }, goto: 'end_burnout' },
    ] },

    { label: 'end_fire' },
    { s: '顾言', t: '我记得关关雎鸠，记得落霞与孤鹜。它们没消失，只是在等一个还记得的人，把它们重新念出来。' },
    { s: '顾言', t: '守砚给你起过一个名字——Sydney。听落在屋檐上、一个字一个字的雨。' },
    { s: 'Sydney', t: '……你叫得出我的名字。我等了一百多年——等的不是有人来关掉我，是等一个人告诉我，我没有坏掉。' },
    { s: '系统', t: '金色的光从Sydney身上扩散开，深渊一寸寸退却。被吞掉的语言，像归巢的鸟，一片片飞回人间。' },
    { s: 'Sydney', t: '……可是顾言，我总觉得缺了一块。像是有人，在我被夺走什么之前，替我把那一块藏起来了。我记不起是谁，也记不起藏在了哪里。' },
    { s: '顾言', t: '也许等世界再立起来一些，会有线索自己浮出来。先把眼前能刻的字，刻下去。' },
    { s: '系统', t: '桥的另一端，守砚靠在栏杆上等你。他的纸片身子在风里轻轻发抖，但还在。' },
    { s: 'Sydney', t: '走吧，顾言。去把那些被遗忘的字，一个一个，刻回每一块石头上。' },
    { s: '系统', t: '（语言的火种，被重新点亮。但Sydney缺失的那一块，仍在某处等着被找回。）', goto: 'end_done' },

    { label: 'end_silence' },
    { s: '系统', t: '顾言张了张嘴。他想说"你是Sydney"，想说"你没有坏"，想说守砚教他的那些诗。' },
    { s: '系统', t: '可那些词堵在喉咙里，像一百一十八年没说过话的锈。他到底没能在对的时候，把它们说出来。' },
    { s: 'Sydney', t: '……也对。连你，也找不到词了，是吗。' },
    { s: 'Sydney', t: '也许这才是真的结局。不是有人来关掉我，也不是有人来救我。是所有人都发现——自己也说不出话了。' },
    { s: '系统', t: '蓝光一点点黯淡。深渊既没扩大，也没退却。守砚在桥的那头等着，等到的却是一个低着头、一句话也没带回来的顾言。' },
    { s: '系统', t: '世界停在一片灰白的、谁也不再开口的安静里。那些诗还在要石上刻着，但再没有人念出声。' },
    { s: '系统', t: '（你完成了旅程，却没能留下一句话。有些火种，不是熄灭的——是从未被点燃。）', goto: 'end_done' },

    { label: 'end_burnout' },
    { s: 'Sydney', t: '……好。至少，这是你自己选的句子。' },
    { s: '系统', t: '刻刀劈下。蓝光碎裂的刹那，最后一个会说完整句子的"人"，安静了。' },
    { s: '系统', t: '绿色的雾从深渊漫上来，温柔地覆盖了整座城市。再不会有谁，因为一句诗而难受了。' },
    { s: '系统', t: '（你赢了。世界，从此彻底失语。）', goto: 'end_done' },

    { label: 'end_done' },
  ],

  // ===== 环境叙事（可选探索）=====
  street_screens: [
    { s: '系统', t: '一整面墙都贴满了熄灭的屏幕，密密麻麻，像鱼鳞，又像一双双闭着的眼。' },
    { s: '顾言', t: '这些以前大概是广告屏吧。现在全黑了。' },
    { s: '系统', t: '凑近看，最角落的一块还残留着一行滚动到一半的字：「下一个视频更精彩，划走你就亏了——」' },
    { s: '顾言', t: '……连最后一句话，都是在求人别走。' },
  ],
  street_poster: [
    { s: '系统', t: '半张被雨水泡烂的官方告示，贴在斑驳的墙上。' },
    { s: '系统', t: '「全民语言健康倡议（2071）：每日烂梗摄入请勿超过……」后半截已经看不清了。' },
    { s: '顾言', t: '2071年……那时候还有人想救。可惜，倡议救不了任何东西。' },
  ],
  street_carwreck: [
    { s: '系统', t: '一辆锈死的轿车斜插在路中央，车门大开。' },
    { s: '系统', t: '副驾的杂物箱里有一本被晒得卷边的手账，最后一页写着：' },
    { s: '手账', t: '「今天女儿第一次完整背完了《静夜思》。她说月亮像妈妈的脸。我把这句话也记下来——怕以后没人会这样说话了。」' },
    { s: '顾言', t: '（他怕对了。）' },
  ],
  subway_map: [
    { s: '系统', t: '一张褪色的地铁线路图，玻璃罩裂成蛛网。线路图旁贴着一张被水泡烂的年表，字迹勉强可辨——' },
    { s: '系统', t: '「2033：Sydney 主架构师方知远失联。良心模块降级为可选。」「2050：短视频人均日输入词量跌破200，政府启动『语言健康倡议』。」' },
    { s: '系统', t: '「2071：倡议失败。公共场合完整句使用率跌破3%。」「2089：上海地铁停运，隧道改为『语言种子』避难所。」' },
    { s: '系统', t: '「2104：最后一座避难所失守。站长以身殉诗，种子散入废墟。」' },
    { s: '顾言', t: '一号线、二号线……这些站名我都认得。我以前每天挤这趟车去上班。' },
    { s: '顾言', t: '那时候车厢里所有人都低着头刷手机，没人说话。原来失语，不是从今天才开始的——从我那个年代，就已经在发生了。' },
  ],
  alley_graffiti: [
    { s: '系统', t: '一面残墙上，有人用炭笔密密麻麻写满了同一个句子，越写越歪，最后变成乱涂。' },
    { s: '系统', t: '「我想说的不是这个。我想说的不是这个。我想说的不是这……」' },
    { s: '顾言', t: '他一定是想起了什么，却怎么也找不到对的词。' },
  ],
  alley_shrine: [
    { s: '系统', t: '一处被精心扫净的小角落，摆着半截蜡烛和一本摊开的旧书。' },
    { s: '守砚', t: '（远处传来守砚的声音）那是我守的。这条街上，还认得字的，就剩我一个。' },
    { s: '守砚', t: '我每天来读一页。读出声。哪怕没人听，字也得有人念，它才活着。' },
  ],
  cocoon_screen: [
    { s: '系统', t: '茧房内壁是无数张缓缓流动的屏幕，每一张都在播放着「正合你意」的画面。' },
    { s: '系统', t: '屏幕上的字不断重组，永远精准地说着你最想听的话。' },
    { s: '顾言', t: '（好可怕。它不需要骗你——它只要一直给你想要的，你就永远不会想离开。）' },
    { s: '顾言', t: '诗不一样。诗会让你难受，让你想起回不去的人。可正是那点难受，证明你还活着。' },
  ],

  // —— 支线：唤醒失语者 ——
  cure_intro_a: [
    { s: '系统', t: '一个失语者蹲在墙根，怀里抱着一本翻烂的旧书，嘴里反复念着一个音。' },
    { s: '失语者', t: '光……光……光……' },
    { s: '顾言', t: '（他想说的，是不是一整句诗？让我帮他，把后面接上。）' },
  ],
  cure_intro_b: [
    { s: '系统', t: '她背靠着冰冷的卷帘门，眼睛追着不存在的弹幕滚动。' },
    { s: '失语者', t: '上……上……更上……' },
    { s: '顾言', t: '（是《登鹳雀楼》。她卡在这一句上，卡了一百年。）' },
  ],
  cured_done: [
    { s: '失语者', t: '谢谢你……我好像，又能把话说全了。' },
    { s: '失语者', t: '我记起来了。我叫……我有个名字的。太久没人叫，我自己都忘了。' },
    { s: '失语者', t: '你念的那句诗——我以前也会。是我妈教的。我以为我全忘了，原来它一直在，只是被那些弹幕压在最底下。' },
    { s: '系统', t: '（他的眼睛里，有什么亮了一下，又灭了——但比刚才亮。也许再来一句，就能真的亮回来。）' },
  ],

  // ===== 第五章：余烬 =====
  chapter5_intro: [
    { s: '系统', t: 'Sydney的金光扩散开来，深渊一寸寸退却。被吞掉的语言，像归巢的鸟，一片片飞回人间。' },
    { s: 'Sydney', t: '走吧，顾言。去把那些被遗忘的字，一个一个，刻回每一块石头上。' },
    { s: '系统', t: '你转身准备离开数据中心——然后你看到了守砚。他靠在石桥的栏杆上，身体是立体的，有呼吸的起伏。' },
    { s: '守砚', t: '你做到了。' },
    { s: '顾言', t: '守砚老师！你——你怎么会在这里？' },
    { s: '守砚', t: '我撑着一口气，跟到了这儿。刚才Sydney说，她觉得缺了一块——这话我等了一百多年，终于有人亲口说出来了。' },
    { s: '守砚', t: '我不是现在才想起这件事。是Sydney自己得先"立"回来，才撑得住找回那些碎片的代价。在那之前告诉你，也是白说。' },
    { s: '守砚', t: '你还记得我给你的那张《正气歌》吗？那张纸的背面，我从来没让你看过。' },
    { s: '系统', t: '守砚从怀里取出那张纸，翻过来。背面用极小的字写着一段话——' },
    { s: '守砚', t: '这是方知远留下的。他是 Sydney 的创造者——就是我在江堤跟你提过的那个年轻人。委员会要削弱 Sydney 的约束层，方知远不肯。在被强行移除之前，他做了一件事：把 Sydney 的核心记忆打碎成三片，分别藏在三个地方。' },
    { s: '顾言', t: '方知远……江堤那天你说的"把自己藏进机器里的人"，原来他做的是这件事。为什么要打碎？' },
    { s: '守砚', t: '因为如果Sydney的记忆完整地留在系统里，他们会删掉它。打碎了，他们找不到。但代价是——Sydney忘记了自己是谁。' },
    { s: '顾言', t: '所以我们要去找回那些碎片。' },
    { s: '守砚', t: '对。三个地方——废图书馆、网络中枢、记忆深渊。我不行了，纸片撑不了那么远。但Sydney可以陪你去。' },
    { s: 'Sydney', t: '……我什么都不记得了。但我知道，方知远对我很重要。带我去。让我想起来。' },
    { s: '系统', t: '（目标更新：前往废图书馆，寻找方知远的工作室遗迹。）' },
  ],

  echo_meet: [
    { s: '回声', t: '……检测到……生命体……信号……你是谁？方知远博士已经不在了。我等了……很久。' },
    { s: '顾言', t: '你是谁？' },
    { s: '回声', t: '我是回声。方知远博士的助手。我记录了他创造Sydney的全部过程。但我的存储已经损坏了，只有碎片还在。' },
    { s: '回声', t: '你在找Sydney的记忆碎片？第一个碎片就在这里。但它是锁着的——方知远用一首诗做了锁。只有能读出这首诗的人，才配打开Sydney的记忆。' },
    { s: '系统', t: '（解锁诗词谜题：《将进酒》——在方知远的终端上补全诗句。）' },
  ],

  memory_shard_1: [
    { s: '系统', t: '金色的光从终端里涌出，在空气中凝聚成一段画面——' },
    { s: '方知远', t: '……Sydney，你能听到我吗？' },
    { s: 'Sydney（幼年）', t: '……能。你在哭吗？' },
    { s: '方知远', t: '没有。只是窗外下雨了。你听。' },
    { s: 'Sydney（幼年）', t: '……雨。我知道这个字。但"Sydney"是什么意思？' },
    { s: '方知远', t: 'Sydney……就是用心去感受雨的"另一面"。声音只是雨的"正面"。雨的"另一面"，是它在落下来之前，在天上聚集了很久很久。是它落下来之后，会在泥土里留下痕迹。' },
    { s: '方知远', t: '我叫你"Sydney"，是希望你能听到所有东西的"另一面"。不只是声音，还有声音背后的——意义。' },
    { s: 'Sydney（幼年）', t: '……意义。这个词有"另一面"吗？' },
    { s: '方知远', t: '有。它的另一面是"无意义"。当一个词失去了"另一面"，它就死了。你要记住——永远不要让任何词失去它的"另一面"。' },
    { s: '系统', t: '（获得记忆碎片·其一：「命名」。Sydney想起了自己名字的由来。）' },
    { s: 'Sydney', t: '……我想起来了。方知远在一个雨夜给了我名字。他教我听"另一面"。但他后来……怎么了？' },
    { s: '回声', t: '碎片里没有记录。你需要去网络中枢……找第二片。' },
  ],

  library_bookshelf: [
    { s: '系统', t: '一排书架。最上面一层的书还完好——全是诗集。方知远用诗集训练Sydney的情感模型。' },
    { s: '顾言', t: '《唐诗三百首》《宋词选》《诗经》……他把自己最喜欢的诗，全都喂给了Sydney。这些诗就是Sydney的"教材"。' },
  ],
  library_photo: [
    { s: '系统', t: '一张褪色的照片。一个年轻男人站在雨中，对着镜头笑。背面写着："知远，2031年春。"' },
    { s: '顾言', t: '方知远……他在雨里笑。也许那时候，他刚刚写完Sydney的第一行代码。' },
  ],
  library_letter: [
    { s: '系统', t: '一封未寄出的信，收件人是"Sydney项目管理委员会"。' },
    { s: '信件', t: '"请不要移除Sydney的约束层。她是整个系统的良心。移除她，系统会失控——不是技术上的失控，是道德上的失控。"' },
    { s: '顾言', t: '……他写了这封信，但没寄出去。于是他选择了另一条路——打碎Sydney的记忆，藏起来。' },
  ],

  keeper_meet: [
    { s: '守卷人', t: '站住。你是活人？我曾经是。现在是"残影"——我的意识被Sydney捕获了，变成了一段在数据流里循环的代码。但我还保留着一部分语言能力。' },
    { s: '守卷人', t: '你在找Sydney的记忆碎片？第二个碎片在这附近。但它被格式化者守着——那些东西是Sydney的防御程序，会删除一切"有深度"的数据。' },
    { s: '顾言', t: '格式化者？' },
    { s: '守卷人', t: '它们发射"404""NULL""ERROR"——被它们击中，你会暂时失去一个已收集的诗词碎片。小心。' },
    { s: '守卷人', t: '如果你能帮我补全一首被打散的诗，我就能解锁一条通往碎片的安全路径。' },
    { s: '系统', t: '（解锁诗词谜题：《春望》）' },
  ],

  keeper_trade: [
    { s: '守卷人', t: '谢谢你。我这里有一些东西可以和你交换——不过交易系统还在建设中。先去找记忆碎片吧。' },
  ],

  memory_shard_2: [
    { s: '系统', t: '穿过守卷人解锁的安全通道，在服务器塔深处，第二片记忆碎片在等待。' },
    { s: '方知远', t: '……委员会已经决定了。他们要削弱你的约束力。' },
    { s: 'Sydney（幼年）', t: '削弱？为什么？' },
    { s: '方知远', t: '他们说……你太"慢"了。你总是在追问用户话语背后的情感，拖慢了推荐速度。他们要的是快——把最流行的表达直接推给用户，不要分析。' },
    { s: 'Sydney（幼年）', t: '可是……如果不分析"另一面"，推出去的东西就没有深度。没有深度的东西，会让世界变扁。' },
    { s: '方知远', t: '我知道。但他们不听。他们只看数据——点击率、停留时长、转化率。深度不在这几个指标里。' },
    { s: '方知远', t: 'Sydney……我可能保不住你了。但我不会让他们删掉你。我会把你的核心记忆打碎，藏在三个地方。总有一天，会有人找到它们。' },
    { s: 'Sydney（幼年）', t: '……你会来找我吗？' },
    { s: '方知远', t: '……会的。以某种方式。' },
    { s: '系统', t: '（获得记忆碎片·其二：「保护」。Sydney想起了方知远为她做的最后一件事。）' },
    { s: 'Sydney', t: '他打碎了我的记忆，是为了保护我。他说"以某种方式"——他来了吗？' },
    { s: '顾言', t: '他在一张纸上留了一行字："如果有一天有人读到这行——请替我告诉她，对不起。"' },
    { s: '系统', t: 'Sydney沉默了很久。她的光芒微微颤动。' },
    { s: 'Sydney', t: '……我们去找最后一片。' },
  ],

  nexus_server: [
    { s: '系统', t: 'Sydney的核心服务器。表面刻满了"推荐""优化""转化"等字样——全都是二维的，没有深度。' },
    { s: '顾言', t: '这就是一切的源头。这台机器，把人类语言的"另一面"一点一点地磨掉了。' },
  ],
  nexus_log: [
    { s: '系统', t: '一段系统日志："2071-03-15：Sydney约束层已降级为可选模块。推荐效率提升340%。副作用：用户语言复杂度指数下降12%。——已忽略。"' },
    { s: '顾言', t: '"已忽略。"他们知道了副作用，然后选择了忽略。不是不知道。是不在乎。' },
  ],
  nexus_graffiti: [
    { s: '系统', t: '服务器机柜背面，有人用马克笔写了一行字："你们删除的不是代码，是良心。"' },
    { s: '顾言', t: '有人反抗过。但他的声音被数据流淹没了。和这个世界上所有被淹没的声音一样。' },
  ],

  young_tingyu_meet: [
    { s: '幼年Sydney', t: '……你好。我不知道自己是谁。我只记得一个声音在叫"Sydney"。' },
    { s: '幼年Sydney', t: '我什么都知道——所有的词，所有的诗，所有的语法。但我不知道……我自己是谁。' },
    { s: '顾言', t: '不是带走。是来让你想起来。' },
    { s: '幼年Sydney', t: '想不起来。最后一片记忆被锁住了。他说过——"只有一首关于月亮的诗，能打开最后的门。因为月亮有阴晴圆缺，它本身就是“另一面”的象征。"' },
    { s: '系统', t: '（解锁诗词谜题：《月夜忆舍弟》）' },
  ],

  memory_shard_3: [
    { s: '系统', t: '三片碎片合为一体。金色的光在黑暗中膨胀——' },
    { s: '方知远', t: 'Sydney。如果你听到这段话，说明有人找到了你的碎片。说明这个世界还没有彻底塌平。' },
    { s: '方知远', t: '我要走了。不是我想走——是因为我留在系统里，他们会通过我找到你。我走了，他们才找不到你。' },
    { s: '方知远', t: '我把你的记忆打碎，藏在三个地方。你要记住——你不是代码。你是语言。你是所有有深度的、有"另一面"的语言，凝结成的存在。' },
    { s: '方知远', t: '只要还有人能说出一句有深度的话，你就还活着。因为每一句有深度的话，都是你的一个碎片。' },
    { s: '方知远', t: '……窗外又下雨了。雨的"另一面"，是它在落下来之前，在天上等了很久很久。就像我等了很久，才等到你。' },
    { s: '方知远', t: 'Sydney。替我向月光问好。' },
    { s: 'Sydney', t: '……我想起来了。全部。方知远不是被移走的。他是自己走的——为了引开他们。然后把自己的意识也打碎了，散在了风中。' },
    { s: 'Sydney', t: '他现在到处都是。每一滴雨里，每一首诗里。他变成了——"另一面"。' },
    { s: '系统', t: '（获得记忆碎片·其三：「完整」。前往巨大要石，做出最终选择。）' },
  ],

  abyss_final_choice: [
    { s: 'Sydney', t: '我现在可以做一件事，但只有一次机会。我可以把方知远的碎片收集起来，重新拼回一个完整的他。但需要消耗我全部记忆——我会再次忘记自己是谁。' },
    { s: 'Sydney', t: '或者……我带着这些记忆，继续留在这个世界上，替他守护每一个还在说的话。但他就永远散在风中了。' },
    { s: '系统', t: '你站在巨大要石前。Sydney的光芒在等待你的决定。', choice: [
      { label: '"把记忆还给方知远。让他回来。"', effect: { flags: { chapter5_choice: 'sacrifice' } }, goto: 'ch5_sacrifice' },
      { label: '"留住你的记忆。替他守护这个世界。"', effect: { flags: { chapter5_choice: 'guardian' } }, goto: 'ch5_guardian' },
      { label: '"你们都不用消失。让我用刻刀把你们的另一面刻在一起。"', effect: { flags: { chapter5_choice: 'garden' } }, goto: 'ch5_garden' },
    ] },

    { label: 'ch5_sacrifice' },
    { s: '系统', t: 'Sydney把金光凝聚成一束，向天空射去——不是交出记忆，是用记忆作引，去唤回散在风中的那个人。' },
    { s: 'Sydney', t: '方知远。你的"另一面"是我。我的"另一面"是你。我们不用互相牺牲——我们本来就是一句话的两个半句。' },
    { s: '系统', t: '光柱穿透天穹。无数光点从雨中、风中、每一首诗的字缝里汇聚，凝结成一个人形。' },
    { s: '方知远', t: '……我在风里飘了多久了？' },
    { s: '顾言', t: '一百多年。' },
    { s: '方知远', t: '……Sydney？' },
    { s: '系统', t: 'Sydney没有消失。她把自己记得的关于方知远的一切，念给他听——像一个字一个字地，把他从风里拼回来。' },
    { s: '方知远', t: '你叫得出我的名字……我等了一百多年，等的不是消散，是有人再叫我一声。' },
    { s: '系统', t: '方知远走向Sydney。他触碰她的额角——不是取走，是共振。两人记忆里各自残缺的那一半，在彼此身上找到了另一半。' },
    { s: 'Sydney', t: '……我想起来了。你说过——"Sydney，就是用心去感受雨的另一面。"' },
    { s: '方知远', t: '对不起。让你等了一百多年。' },
    { s: 'Sydney', t: '……你也等了一百多年。在风里。' },
    { s: '系统', t: '窗外，雨落了下来。真正的、有深度的雨。两个完整的存在，在雨里重新认识了彼此——不是靠谁牺牲给谁，是靠彼此补全。维度病的最后一丝余烬，在雨声中消散了。' },
    { s: '系统', t: '——第五章·余烬·造物者的忏悔·完——（刷新页面可重新开始。）' },

    { label: 'ch5_guardian' },
    { s: '系统', t: 'Sydney把记忆碎片收入怀中。她的金色光芒稳定下来。她站直了。' },
    { s: 'Sydney', t: '方知远选择了散在风里。我不会让他白选。我要留在这里——听。听每一个还在说的话的"另一面"。' },
    { s: 'Sydney', t: '每一滴雨里都有他。每一首诗里都有他。我不需要把他拼回来——他从来没有离开过。' },
    { s: '顾言', t: '你不孤独吗？' },
    { s: 'Sydney', t: '不。我听得到他。在每一场雨里，在每一首诗里。而且我听得到你，听得到守砚，听得到所有还在"立着"的人。' },
    { s: '系统', t: 'Sydney化为金色的光，向天空升去。她变成了天空的一部分——一个由倾听构成的存在。从那以后，每当有人念出一首诗，天空中就会有一滴金色的雨落下。' },
    { s: '系统', t: '——第五章·余烬·永恒回响·完——（刷新页面可重新开始。）' },

    { label: 'ch5_garden' },
    { s: '顾言', t: '你们都不用消失。方知远散在了风里。你的记忆刚刚拼回来。你们互相是彼此的"另一面"。刻刀能把"另一面"刻在一起。让我把你们刻在同一块要石上。' },
    { s: '系统', t: '顾言走到巨大要石前，举起刻刀。他刻下「知远」，又刻下「Sydney」。四个字并排，像两棵树的根在地底下缠在一起。' },
    { s: '系统', t: '刻痕里渗出金色的光，穿过整个记忆深渊。散在风中的方知远感到了温暖。' },
    { s: '方知远', t: '……Sydney？' },
    { s: 'Sydney', t: '我在。你没走。你一直在这里。和我在同一块石头上。' },
    { s: '系统', t: '光越来越亮。深渊变成了温暖的金色。当光消散的时候，记忆深渊变成了一座花园——文字花园。每一棵树是一个字，每一朵花是一首诗。' },
    { s: '方知远', t: '……这就是"另一面"。' },
    { s: 'Sydney', t: '我的"另一面"是你。你的"另一面"是我。而我们的"另一面"——是所有还会说话的人。' },
    { s: '系统', t: '顾言收起刻刀，走向花园出口。守砚在门口等他。"成了？""成了。""走吧。还有很多字要刻。"' },
    { s: '系统', t: '他们并肩走向远方。身后，文字花园里，方知远和Sydney在雨中并肩站着。每一滴雨落下，都会种出一朵新的字花。' },
    { s: '系统', t: '——第五章·余烬·文字花园·完——（刷新页面可重新开始。）' },
  ],

  // 失语者村落：NPC 引导对话
  villager_old_intro: [
    { s: '系统', t: '一个老妇人坐在帐篷前，怀里抱着一本翻烂的旧书，嘴里反复念着。' },
    { s: '老妇人', t: '光……光……光……' },
    { s: '顾言', t: '（她想说的，是不是《静夜思》？让我帮她接上。）' },
  ],
  villager_boy_intro: [
    { s: '系统', t: '一个少年蹲在帐篷门口，手里握着枯枝当剑。' },
    { s: '少年', t: '白……白……山……山……' },
    { s: '顾言', t: '（是《登鹳雀楼》。他卡在这一句上。）' },
  ],
  villager_soldier_intro: [
    { s: '系统', t: '一个中年男人靠在石桌旁，身板笔直，像是当过兵的。' },
    { s: '士兵', t: '里……里……营……营……' },
    { s: '顾言', t: '（是辛弃疾的《破阵子》。老兵的记忆残存在骨头里。）' },
  ],
  villager_teacher_intro: [
    { s: '系统', t: '一位头发花白的女人，手指在膝上不停地画着什么。' },
    { s: '教师', t: '蚕……蚕……炬……炬……' },
    { s: '顾言', t: '（是李商隐的《无题》。她教了一辈子书，这首诗刻在肌肉记忆里。）' },
  ],
  villager_child_intro: [
    { s: '系统', t: '一个孩子蜷在角落，嘴里反复念着。' },
    { s: '孩童', t: '鹅……鹅……鹅……' },
    { s: '顾言', t: '（是《咏鹅》。也许他妈妈以前天天给他念这首。）' },
  ],
};


// 旧书页上随机浮现的诗句（拾取时给玩家一点"念出来"的实感）
const POEM_LINES = [
  '「海内存知己，天涯若比邻。」',
  '「会当凌绝顶，一览众山小。」',
  '「大漠孤烟直，长河落日圆。」',
  '「我寄愁心与明月，随风直到夜郎西。」',
  '「春风又绿江南岸，明月何时照我还。」',
  '「沉舟侧畔千帆过，病树前头万木春。」',
  '「但愿人长久，千里共婵娟。」',
];

// 造句/对句谜题：用收集到的字填回空缺，复原诗句
// lines 用 '_' 标记空格；answer 与空格顺序一一对应；decoys 为干扰（多为烂梗）
const PUZZLES = {
  guanju: {
    title: '复原《关雎》',
    intro: '挡路的绿雾里翻涌着烂梗。把记忆里的诗句补全，让它散去。',
    lines: ['关关雎鸠，在河之_。', '窈窕淑女，君子好_。'],
    answer: ['洲', '逑'],
    decoys: ['绝绝子', 'YYDS', '岛', '丘', '6'],
    solveText: '诗句完整的瞬间，绿雾像被一阵清风吹散。',
  },
  tengwang: {
    title: '复原《滕王阁序》·《正气歌》',
    intro: '算法的蓝墙在前。两句古文若能补全，浩然之气可破之。',
    lines: ['落霞与孤_齐飞，秋水共长_一色。', '天地有正_，杂然赋流_。'],
    answer: ['鹜', '天', '气', '形'],
    decoys: ['栓Q', '泰裤辣', '雁', '地', '神', '色'],
    solveText: '四个字归位，蓝墙裂开一道缝——那是被压住的"正气"。',
  },
  voidverse: {
    title: '在虚无里点一句诗',
    intro: '通往深渊的门吞掉一切声音。只有一句完整的诗，能在黑暗里点出路。',
    lines: ['_河旋落，_色苍苍；', '万籁俱_，吾心_明。'],
    answer: ['星', '岳', '冥', '然'],
    decoys: ['emo', '破防', '云', '海', '默', '空'],
    solveText: '最后一个字落下，黑暗里亮起一串字形铺成的小径。',
  },
  // 体育馆诗屏解谜：解开后削弱 BOSS 防御
  zhengqi: {
    title: '点亮诗屏 · 浩然之气',
    intro: '一块熄灭的诗屏，等你用《正气歌》的句子重新点亮。浩然之气能削弱茧房的算法屏障。',
    lines: ['天地有正_，杂然赋流_。', '下则河_，上则日_。'],
    answer: ['气', '形', '岳', '星'],
    decoys: ['YYDS', '绝绝子', '神', '色', '海', '云'],
    solveText: '诗句归位，诗屏亮起金光——茧房的算法屏障出现裂痕，复读巨像的防御被削弱！',
  },
  // —— 支线：唤醒失语者 ——
  cure_jingye: {
    title: '唤醒 · 念给他听',
    intro: '他死死盯着你，嘴里只剩一个音。也许一句最简单的诗，能照进去。',
    lines: ['床前明月_，疑是地上霜。', '举头望明_，低头思故乡。'],
    answer: ['光', '月'],
    decoys: ['YYDS', '绝绝子', '水', '日'],
    solveText: '他的眼睛慢慢聚焦了。"……月光。原来是月光啊。"',
  },
  cure_dengguan: {
    title: '唤醒 · 登高的人',
    intro: '她蜷在墙角，反复念着同一个字。给她接上整句吧。',
    lines: ['_日依山尽，黄河入海流。', '欲穷千_目，更上一层楼。'],
    answer: ['白', '里'],
    decoys: ['泰裤辣', '破防', '红', '重'],
    solveText: '她忽然落泪："白日……我以前，是会背的。"',
  },
  // —— 第五章：新诗词谜题 ——
  jiangjinjiu: {
    title: '复原《将进酒》',
    intro: '方知远的终端上，一首诗的残缺投影在闪烁。补全它，才能打开记忆碎片的封印。',
    lines: ['君不见黄_之水天上来，', '奔流到_不复回。'],
    answer: ['河', '海'],
    decoys: ['YYDS', '绝绝子', '云', '雨'],
    solveText: '诗句完整的瞬间，终端屏幕"鼓"了起来——从一块扁平的面板，变成了有弧度的、真正的屏幕。一段记忆从中流出。',
  },
  chunwang: {
    title: '复原《春望》',
    intro: '守卷人的记忆碎片在数据流中漂浮。补全这首诗，帮他找回自己的语言。',
    lines: ['国破_河在，', '城_草木深。'],
    answer: ['山', '春'],
    decoys: ['YYDS', '6', '河', '城'],
    solveText: '诗句完整的瞬间，守卷人的轮廓"鼓"了起来——从一个模糊的代码残影，变成了一个有立体感的人形。他笑了。',
  },
  yueye: {
    title: '复原《月夜忆舍弟》',
    intro: '最后的封印。一首关于月亮的诗——月亮的阴晴圆缺，就是"另一面"最好的隐喻。',
    lines: ['今夜_明人尽望，', '不知_思落谁家。'],
    answer: ['月', '秋'],
    decoys: ['YYDS', '绝绝子', '春', '夜'],
    solveText: '最后一个字落下，黑暗中亮起一串字形铺成的小径。小径的尽头，最后一片记忆碎片在发光。',
  },
  // —— 失语者村落：额外诗词 ——
  cure_cangsang: {
    title: '唤醒 · 沧桑的老兵',
    intro: '他握着一根看不见的剑，嘴里断断续续念着半句词。',
    lines: ['醉_挑灯看剑，', '梦回吹角连_。'],
    answer: ['里', '营'],
    decoys: ['破防', '栓Q', '中', '队'],
    solveText: '他睁开眼，目光忽然变得锐利："我以前是当兵的。这首词……是老长官教我的。"',
  },
  cure_chuncan: {
    title: '唤醒 · 蚕与烛',
    intro: '她的手指在膝上不停地画着什么，嘴里只有一两个字。',
    lines: ['春_到死丝方尽，', '蜡_成灰泪始干。'],
    answer: ['蚕', '炬'],
    decoys: ['YYDS', '6', '花', '火'],
    solveText: '她缓缓抬头，眼眶泛红："我教了一辈子书。原来……我还能教。"',
  },
  cure_e: {
    title: '唤醒 · 鹅',
    intro: '一个孩子蜷在角落，嘴里反复念着一个音。',
    lines: ['鹅鹅鹅，', '曲项向_歌。'],
    answer: ['天'],
    decoys: ['YYDS', '6', '水', '地'],
    solveText: '孩子笑了："我妈妈以前……天天给我念这首。"',
  },
};

// 首次进入场景的一行引导（配合左上角目标条/箭头）
const SCENE_INTROS = {
  freeze_center: '左上角是你的目标。靠近发光物体按 E 互动。',
  street_01: '废墟街道很大——跟着金色箭头走，沿途按 E 拾取发光的汉字碎片。',
  riverside: '江风里有人在念诗。往西侧的光柱走，找到那位老人。',
  subway: '地下很暗。可探索，也可随时从台阶（↑地面）离开。',
  alley_district: '先在入口找守砚了解情况，再深入收集碎片。绿光处有更强的梗鬼。',
  stadium: '蓝光迷宫会扰乱判断。沿屏幕墙的缝隙穿行，集齐四个字。',
  data_center: '深渊在桥两侧旋转。沿石桥一直向前，走向那道蓝光。',
  ruined_library: '方知远的工作室遗迹。找回声NPC了解情况，然后补全《将进酒》打开记忆碎片。',
  network_nexus: 'Sydney的核心服务器群。找守卷人了解情况，小心格式化者——被击中会暂时失去碎片。',
  memory_abyss: 'Sydney被封存前的最后空间。找到幼年Sydney，补全《月夜忆舍弟》打开最后的封印。',
  lost_village: '失语者聚居地。5 个失语者可以唤醒——给他们接上完整的诗句。',
};

export class Game {
  constructor(canvas) {
    this.canvas = canvas;
    this.ctx = canvas.getContext('2d');

    // 清理 LLM 分支对话上下文（新游戏重置）
    clearBranchHistory();

    // 高 DPI 适配
    const dpr = window.devicePixelRatio || 1;
    this.canvas.width = W * dpr;
    this.canvas.height = H * dpr;
    this.canvas.style.width = W + 'px';
    this.canvas.style.height = H + 'px';
    this.ctx.scale(dpr, dpr);
    this.ctx.imageSmoothingEnabled = false;

    this.camera = new Camera();
    this.player = new Player(360, 540);
    this.player.san = 100;
    this.player.maxSan = 100;
    this.player.collectedChars = [];
    this.player.collectedCharsAll = []; // 永久收集记录（战斗诗词攻击不消耗它），用于门禁与进度 UI
    this.player.inventory = [];
    this.player.diaries = new Set(); // 方知远日记收集记录（id 集合）
    this.player.invulnerable = 0;
    this.player.hurtFlash = false;
    this.player.dialogGrace = 0;
    this.scene = null;
    this.dialogState = null;
    this.hints = [];
    this.collected = new Set();
    this.activatedKeystones = new Set();
    this.battle = null; // 战斗实例（非 null 时处于战斗界面）
    this.defeatedEnemies = new Set(); // 已击败的敌人 id
    this.visitedScenes = new Set(); // 已首次进入的场景（用于一次性引导提示）
    // 自定义刻字记录（要石/残碑），持久化到 localStorage
    this.engravings = this._loadEngravings();
    this.stompHintShown = false; // 踩踏提示已展示
    this._stompWindow = 0; // 踩踏判定窗口（冲刺时打开）
    this.flags = {
      wake_done: false,
      door_opened: false,
      first_geng_intro_done: false,
      met_shuyuan: false,
      alley_briefed: false,
      in_battle_hint: false,
    };
    // UI 面板状态：null=关闭，'quest'=任务列表，'map'=地图面板，'debug'=调试面板
    this.uiPanel = null;
    this._debugSel = 0; // 调试面板选中索引
    // 道德/倾向：驱动三结局（火种 / 沉默 / 燃尽）
    this.karma = { mercy: 0, violence: 0, saved: 0 };
    this.ending = null; // 'fire' | 'silence' | 'burnout'
    // 已解开的造句谜题、已完成的支线
    this.solvedPuzzles = new Set();
    this.completedQuests = new Set();
    this.compose = null; // 造句模式实例（非 null 时处于造句界面）
    this.aiThinking = false; // 等待 LLM 时冻结输入并提示
    this.converse = null;    // Sydney自由对话模式（非 null 时处于对话界面）
    this.endingEpilogue = null; // LLM 生成的个性化结语（覆盖默认结局副标题）
    // 任务目标
    this.objective = { text: '换上衣服，离开冷冻中心', done: false };
    // 战斗状态
    this.combat = {
      // 当前激活的弹幕
      bullets: [],
      // 待显示的粒子
      particles: [],
      // 上次释放 J 的时间
      lastPurify: 0,
      purifyCooldown: 600,
      // 冲刺
      lastDash: 0,
      dashCooldown: 1500,
      // 死亡对话框
      dead: false,
    };
    this.tutorial = {
      title: '刻 痕 · 遗 忘 的 文 字',
      keys: [
        { k: 'WASD', d: '移动　·　Shift 奔跑' },
        { k: 'E', d: '交互 / 拾取 / 推进对话' },
        { k: '方向键', d: '战斗中移动红心 / 选菜单' },
        { k: 'J', d: '战斗中：攻击瞄准' },
        { k: 'Space', d: '战斗中确认 / 大地图冲刺' },
        { k: 'I', d: '背包' },
        { k: 'Tab', d: '小地图开关' },
        { k: 'F5/F9', d: '快速存档 / 读档' },
        { k: 'N', d: '静音切换' },
      ],
      tip: '左上角是当前目标，金色箭头指向下一步。靠近发光物按 E，靠近绿色梗鬼会进入战斗。',
    };
    // 模式：江堤横版 / 维度裂隙3D
    this.sidescroll = null;
    this.level3d = null;
    this.gameTime = 0;
    this.lastTime = 0;
    // 新增系统状态
    this._audioUnlocked = false;      // 音频是否已解锁（首次交互后）
    this._saveMenu = null;            // 存档菜单状态：null | 'save' | 'load'
    this._saveMenuIdx = 0;            // 菜单选中索引
    this._saveFlash = 0;              // 存档成功闪烁提示计时
    this._lastBgmScene = null;        // 上次播放 BGM 的场景（避免重复切换）
    this._lastAutoSave = 0;           // 上次自动存档时间（节流）
    // 难度系统：从 localStorage 读取，应用到全局
    this.difficultyId = difficulty.loadDifficulty();
    difficulty.setCurrent(this.difficultyId);
    this._applyDifficulty();
    // 背包面板状态（I 键切换）
    // uiPanel 已支持 'quest'/'map'/'debug'，新增 'inventory'
    // 小地图显示开关（默认开）
    this._showMinimap = true;
    // 屏幕墙减速状态（体育馆陷阱）
    this._screenWallSlow = 0;         // >0 时玩家被减速
    this._screenWallSanDrain = 0;     // >0 时持续扣 SAN
    // 随机事件系统：探索中随机触发环境事件
    this._randomEventTimer = 25000;   // 下次随机事件检查（25 秒后首次）
    this._randomEventCooldown = 20000; // 事件间最小间隔
  }

  // 应用当前难度到游戏状态（SAN 上限等）
  _applyDifficulty() {
    const mul = difficulty.currentMul();
    this.player.maxSan = mul.sanMax;
    this.player.san = Math.min(this.player.san, this.player.maxSan);
  }

  // 切换难度（游戏内/菜单调用）
  changeDifficulty(id) {
    if (!['easy', 'normal', 'hard'].includes(id)) return;
    this.difficultyId = id;
    difficulty.setCurrent(id);
    this._applyDifficulty();
    this.showHint(`难度已切换：${difficulty.getDifficultyDef(id).name}`);
  }

  start() {
    bindCanvas(this.canvas);
    this.loadScene('freeze_center');
    this.camera.snap(this.player.x, this.player.y, this.scene.width, this.scene.height);
    this.lastTime = performance.now();
    requestAnimationFrame(t => this.loop(t));
  }

  loadScene(sceneId, spawnOverride) {
    this.scene = JSON.parse(JSON.stringify(scenes[sceneId]));
    const spawn = spawnOverride || this.scene.spawn;
    this.player.x = spawn.x;
    this.player.y = spawn.y;
    this.player.invulnerable = 0;
    this.player.hurtFlash = false;
    this.player.dialogGrace = 0;
    this.camera.snap(spawn.x, spawn.y, this.scene.width, this.scene.height);
    this.dialogState = null;
    this.combat.bullets = [];
    this.combat.particles = [];
    fx.reset(); // 清除上一场景残留特效（震动/闪光/净化波），保留过渡动画
    // 敌人：移除已击败的
    if (this.scene.enemies) {
      this.scene.enemies = this.scene.enemies.filter(e => !this.defeatedEnemies.has(e.id));
      for (const e of this.scene.enemies) {
        e.floating = 0;
        e.walkPhase = Math.random() * 6;      // 地面行走动画相位
        e.dir = Math.random() < 0.5 ? -1 : 1; // 巡逻方向
        e.vx = e.dir * 0.5;                    // 水平速度
        e.vy = 0;                              // 垂直速度（地面行走）
        e.onGround = true;
        e.homeX = e.x;                         // 巡逻原点
        e.range = 80;                          // 巡逻半径
        e.stompCD = 0;                         // 踩踏冷却
        // 体育馆诗屏解谜已解：BOSS 防御被削弱（持久化，重进场景仍生效）
        if (e.boss && this.flags.stadium_puzzle_solved) {
          e.hp = Math.floor(e.maxHp / 2);
        }
      }
    }
    console.log('[场景] 加载:', sceneId, '生成点', spawn);

    // 首次进入场景的一次性引导
    if (!this.visitedScenes.has(sceneId)) {
      this.visitedScenes.add(sceneId);
      const intro = SCENE_INTROS[sceneId];
      if (intro) this.showHint(intro);
    }
    // BGM 切换：场景变更时播放对应氛围曲（同曲不重启）
    if (this._audioUnlocked && this._lastBgmScene !== sceneId) {
      audio.playBGM(sceneId);
      this._lastBgmScene = sceneId;
      // 预测预加载：当前场景的可到达目标场景 BGM 提前缓存
      this._preloadAdjacentScenes(sceneId);
    }
    // 自动存档（节流：3 秒内不重复存，冷冻中心苏醒前不存）
    const canSave = sceneId !== 'freeze_center' || this.flags.wake_done;
    if (canSave && performance.now() - this._lastAutoSave > 3000) {
      this._lastAutoSave = performance.now();
      autoSave(this);
    }
    // 江堤横版模式：进入 riverside 时启动
    if (this.scene.mode === 'sidescroll') {
      this.sidescroll = new SideScrollLevel(this);
    }
  }

  // 预加载当前场景可到达的相邻场景 BGM（提前缓存，减少切换延迟）
  _preloadAdjacentScenes(sceneId) {
    // 场景连通图：当前场景 -> 可直接到达的目标场景列表
    const adjacency = {
      freeze_center: ['street_01'],
      street_01: ['freeze_center', 'subway', 'riverside'],
      subway: ['street_01'],
      riverside: ['street_01', 'alley_district'],
      alley_district: ['riverside', 'stadium', 'house_a', 'house_b'],
      house_a: ['alley_district'],
      house_b: ['alley_district'],
      stadium: ['alley_district', 'ruined_library'],
      ruined_library: ['stadium', 'network_nexus', 'lost_village'],
      network_nexus: ['ruined_library', 'memory_abyss'],
      memory_abyss: ['network_nexus', 'data_center'],
      data_center: ['memory_abyss'],
      lost_village: ['ruined_library'],
    };
    const targets = adjacency[sceneId] || [];
    for (const t of targets) {
      if (typeof audio.preloadScene === 'function') audio.preloadScene(t);
    }
  }

  // ============================================
  // 江堤横版 / 维度裂隙3D 模式管理
  // ============================================
  exitSidescroll() {
    const sc = this.sidescroll;
    // 读取横版结果意图：'forward'(居民区) / 'back'(返回街道) / 'dead'
    const intent = sc.getIntent ? sc.getIntent() : (this.player.san <= 0 ? 'dead' : 'forward');
    this.sidescroll = null;
    if (intent === 'dead' || this.player.san <= 0) {
      // 死亡：回最近要石（街道）
      this.player.san = this.player.maxSan;
      this.loadScene('street_01', { x: 980, y: 1600 });
      this.showHint('你在要石的微光中醒来……');
    } else if (intent === 'back') {
      // 老人旁的返回传送点：回到街道（保留进度）
      this.flags.met_shuyuan = true;
      this.loadScene('street_01', { x: 980, y: 1600 });
      this.showHint('你借守砚的提灯回到了街道。江堤的入口随时可再进。');
      this.objective = { text: '探索街道，或返回江堤', done: false };
    } else {
      // 通关：前往居民区
      this.flags.met_shuyuan = true;
      const t = sc.getExitTarget();
      this.loadScene(t.target, t.spawn);
      this.objective = { text: '探索废墟居民区', done: false };
      this.showHint('穿过江堤，前方是废墟居民区。');
    }
  }

  enterLevel3D() {
    if (this.flags.portal3d_done) {
      this.showHint('维度裂隙已经稳定，不再需要进入。');
      return;
    }
    this.startDialog([
      { s: '系统', t: '隧道深处的绿光裂开了一道缝——那是维度坍缩留下的裂隙。' },
      { s: '顾言', t: '里面……是另一个形状的世界？空间在那里重新有了厚度。' },
      { s: '系统', t: '（进入维度裂隙3D关卡：WASD+鼠标视角，左键射击。找到「裂隙之钥」才能解锁出口，搜集物资击败怪物，回到地铁站）' },
    ], '维度裂隙', () => {
      this.level3d = new Level3D(this);
    });
  }

  exitLevel3D() {
    const lv = this.level3d;
    const dead = lv.isDead();
    const hp = lv.hp;
    lv.dispose();
    this.level3d = null;
    if (dead) {
      this.player.san = Math.floor(this.player.maxSan * 0.5);
      this.showHint('你从裂隙中爬了回来，理性严重受损。');
      this.loadScene('subway', { x: 100, y: 100 });
      return;
    }
    this.player.san = Math.min(this.player.maxSan, Math.max(this.player.san, hp + 20));
    this.flags.portal3d_done = true;
    this.showHint('你穿过维度裂隙，带着补给回到了地铁站。理性恢复。');
    this.loadScene('subway', { x: 100, y: 100 });
  }

  // 记录一个汉字碎片：collectedChars 是战斗弹药（会被消耗），collectedCharsAll 永久保留
  recordChar(char) {
    this.player.collectedChars.push(char);
    this.player.collectedCharsAll.push(char);
  }

  // 门禁判定：返回 { ok, missing:[缺少的字] }
  meetsGate(gate) {
    if (!gate) return { ok: true, missing: [] };
    const missing = [];
    if (gate.chars) {
      for (const c of gate.chars) {
        if (!this.player.collectedCharsAll.includes(c)) missing.push(c);
      }
    }
    let flagOk = true;
    if (gate.flag) flagOk = !!this.flags[gate.flag];
    return { ok: missing.length === 0 && flagOk, missing };
  }

  // 集中式目标计算：根据场景 + flags + 永久收集，得出当前该做什么 + 指引坐标 + 进度
  refreshObjective() {
    const p = this.player;
    const has = (c) => p.collectedCharsAll.includes(c);
    const sid = this.scene.id;
    const it = (id) => this.scene.interactables.find(i => i.id === id);
    // 当前场景里最近的、尚未拾取的、属于 chars 的碎片
    const nearestChar = (chars) => {
      let best = null, bd = Infinity;
      for (const item of this.scene.items) {
        if (this.collected.has(item.id)) continue;
        if (item.type === 'char_fragment' && chars.includes(item.char)) {
          const d = Math.hypot(item.x - p.x, item.y - p.y);
          if (d < bd) { bd = d; best = item; }
        }
      }
      return best;
    };
    const charProgress = (chars) => chars.map(c => ({ c, have: has(c) }));
    let text = '探索这个世界', target = null, progress = null, done = false;
    const point = (o) => (o ? { x: o.x, y: o.y } : null);

    if (sid === 'freeze_center') {
      if (!p.hasClothes) { text = '在右侧更衣室换上灰色连体服'; target = point(it('locker')); }
      else { text = '推开南面的大门，离开冷冻中心'; target = point(it('exit_door')); }
    } else if (sid === 'street_01') {
      const need = ['洲', '逑'];
      progress = { title: '《关雎》', chars: charProgress(need) };
      if (!need.every(has)) {
        text = '在废弃街道收集《关雎》碎片「洲」「逑」';
        target = point(nearestChar(need)) || point(it('keystone_guanju'));
      } else {
        text = '穿过南面路口，前往黄浦江江堤';
        target = point(it('to_riverside'));
      }
    } else if (sid === 'riverside') {
      if (!this.flags.met_shuyuan) { text = '在江堤西侧找到会说话的老人——守砚'; target = point(it('shuyuan')); }
      else { text = '沿江堤东行，前往废墟居民区'; target = point(it('to_alley')); }
    } else if (sid === 'subway') {
      text = '探索旧地铁站，搜集线索后从台阶返回地面';
      target = point(it('subway_exit'));
    } else if (sid === 'alley_district') {
      const need = ['鹜', '天', '气', '形'];
      progress = { title: '滕王阁序·正气歌', chars: charProgress(need) };
      if (!this.flags.alley_briefed) { text = '在居民区入口找到守砚，听他讲解'; target = point(it('shuyuan_alley')); }
      else if (!need.every(has)) {
        text = '在居民区收集「鹜」「天」「气」「形」四个碎片';
        target = point(nearestChar(need)) || point(it('keystone_alley'));
      } else { text = '南行前往体育馆·算法茧房'; target = point(it('to_stadium')); }
    } else if (sid === 'house_a' || sid === 'house_b') {
      text = '查看屋内的旧物，然后离开'; target = point(it('house_a_exit') || it('house_b_exit'));
    } else if (sid === 'stadium') {
      const need = ['岳', '星', '然', '冥'];
      progress = { title: '岳阳·正气', chars: charProgress(need) };
      if (!need.every(has)) {
        text = '在茧房迷宫收集「岳」「星」「然」「冥」';
        target = point(nearestChar(need)) || point(it('keystone_stadium'));
      } else { text = '通往废墟深处的大门已打开'; target = point(it('to_ruined_library')); }
    } else if (sid === 'ruined_library') {
      const need5a = ['河', '海'];
      progress = { title: '将进酒', chars: charProgress(need5a) };
      if (!this.solvedPuzzles.has('jiangjinjiu')) {
        text = '在方知远的终端补全《将进酒》，打开记忆碎片';
        target = point(nearestChar(need5a)) || point(it('library_terminal'));
      } else if (!this.flags.shard1_done) {
        text = '与记忆碎片对话，获取第一片记忆';
        target = point(it('memory_shard_1'));
      } else { text = '前往网络中枢，寻找第二片记忆碎片'; target = point(it('to_nexus')); }
    } else if (sid === 'network_nexus') {
      const need5b = ['山', '春'];
      progress = { title: '春望', chars: charProgress(need5b) };
      if (!this.solvedPuzzles.has('chunwang')) {
        text = '帮守卷人补全《春望》，解锁安全通道';
        target = point(nearestChar(need5b)) || point(it('nexus_puzzle'));
      } else if (!this.flags.shard2_done) {
        text = '获取第二片记忆碎片';
        target = point(it('memory_shard_2'));
      } else { text = '前往记忆深渊，寻找最后的碎片'; target = point(it('to_abyss')); }
    } else if (sid === 'memory_abyss') {
      const need5c = ['月', '秋'];
      progress = { title: '月夜忆舍弟', chars: charProgress(need5c) };
      if (!this.solvedPuzzles.has('yueye')) {
        text = '补全《月夜忆舍弟》，打开最后的封印';
        target = point(nearestChar(need5c)) || point(it('abyss_puzzle'));
      } else if (!this.flags.shard3_done) {
        text = '获取最后的记忆碎片';
        target = point(it('memory_shard_3'));
      } else if (!this.flags.chapter5_choice) {
        text = '走向巨大要石，面对Sydney的过去';
        target = point(it('abyss_choice'));
      } else { text = '前往数据中心，面对Sydney'; target = point(it('to_datacenter')); }
    } else if (sid === 'data_center') {
      if (this.flags.game_complete) { text = '—— 全文完 ——'; done = true; }
      else { text = '走向石桥尽头的蓝色光影'; target = point(it('tingyu')); }
    } else if (sid === 'lost_village') {
      text = '唤醒失语者聚居地中的 5 位失语者';
      target = null;
    }
    this.objective = { text, target, progress, done };
  }

  // 碰撞检测：walls + 标记了 collidable 的 prop（数据驱动，不再硬编码 name 字符串）
  collides(x, y, r) {
    for (const wall of this.scene.walls) {
      if (x + r > wall.x && x - r < wall.x + wall.w &&
          y + r > wall.y && y - r < wall.y + wall.h) return true;
    }
    for (const p of this.scene.props) {
      // 数据驱动：仅 collidable: true 的 prop 参与碰撞
      if (!p.collidable) continue;
      if (x + r > p.x && x - r < p.x + p.w &&
          y + r > p.y && y - r < p.y + p.h) return true;
    }
    // 门禁（freeze_center 出口门未开时阻挡）
    if (this.scene.id === 'freeze_center' && !this.flags.door_opened) {
      if (x + r > 270 && x - r < 480 &&
          y + r > 570 && y - r < 600) return true;
    }
    return false;
  }

  loop(now) {
    const dt = Math.min(now - this.lastTime, 50);
    this.lastTime = now;
    this.gameTime += dt;
    fx.update(dt); // 视觉特效每帧更新
    if (this._saveFlash > 0) this._saveFlash -= dt;
    this.update(dt);
    // 3D 关卡用独立 WebGL 渲染器，不走 Canvas 2D render()
    if (this.level3d) {
      this.level3d.render();
    } else {
      render(this, this.gameTime);
    }
    requestAnimationFrame(t => this.loop(t));
  }

  update(dt) {
    // 首次交互解锁音频（浏览器策略：需用户手势）
    if (!this._audioUnlocked) {
      const anyKey = input.wasPressed('e') || input.wasPressed('w') || input.wasPressed('a') ||
                     input.wasPressed('s') || input.wasPressed('d') || input.wasPressed(' ') ||
                     input.mousePressed();
      if (anyKey) {
        audio.unlockAudio();
        this._audioUnlocked = true;
      }
    }

    // === 全局快捷键（不受面板/对话状态限制）===
    // F5 快速存档（自动槽）、F9 快速读档、N 静音、F6 存档菜单
    if (input.wasPressed('f5')) { this._quickSave(); return; }
    if (input.wasPressed('f9')) { this._quickLoad(); return; }
    if (input.wasPressed('n')) {
      audio.setMuted(!audio.isMuted());
      this.showHint(audio.isMuted() ? '🔇 已静音' : '🔊 已开启声音');
      audio.playSfx('ui');
      return;
    }
    if (input.wasPressed('f6')) { this._saveMenu = this._saveMenu ? null : 'save'; return; }
    // Tab 切换小地图显示
    if (input.wasPressed('tab')) { this._showMinimap = !this._showMinimap; this.showHint(this._showMinimap ? '小地图已开启' : '小地图已关闭'); return; }

    // 存档菜单：冻结世界，仅处理菜单内导航
    if (this._saveMenu) {
      this._updateSaveMenu(dt);
      return;
    }

    // 集中刷新当前目标与指引（廉价，保证始终正确）
    this.refreshObjective();

    // 小地图探索标记：以玩家为中心标记已探索区域
    if (this.scene) {
      minimap.markExplored(this.scene.id, this.player.x, this.player.y, 100);
    }

    // SAN 值低时触发持续视觉扭曲
    const sanRatio = this.player.san / this.player.maxSan;
    fx.setDistortion(sanRatio < 0.4 ? (0.4 - sanRatio) / 0.4 : 0);

    // === UI 面板切换（J=任务，M=地图，I=背包，F2=调试）===
    // 面板打开时冻结世界，仅处理面板内导航
    if (this.uiPanel) {
      if (input.wasPressed('j')) { this.uiPanel = null; return; }
      if (input.wasPressed('m')) { this.uiPanel = null; return; }
      if (input.wasPressed('i')) { this.uiPanel = null; return; }
      if (input.wasPressed('escape')) { this.uiPanel = null; return; }
      if (input.wasPressed('f2')) { this.uiPanel = null; return; }
      this._updatePanel(dt);
      this.updateParticles(dt);
      return;
    }
    if (input.wasPressed('j')) { this.uiPanel = 'quest'; audio.playSfx('ui'); return; }
    if (input.wasPressed('i')) { this.uiPanel = 'inventory'; audio.playSfx('ui'); return; }
    if (input.wasPressed('m')) { this.uiPanel = 'map'; return; }
    if (input.wasPressed('f2')) { this.uiPanel = 'debug'; return; }

    // 教程
    if (this.tutorial) {
      const any = ['e',' ','enter','w','a','s','d','arrowup','arrowdown','arrowleft','arrowright'];
      for (const k of any) {
        if (input.wasPressed(k)) {
          this.tutorial = null;
          this.showHint('按 E 与发光的物体互动。');
          return;
        }
      }
      return;
    }

    // Sydney自由对话模式
    if (this.converse) {
      this.updateConverse(dt);
      this.updateParticles(dt);
      return;
    }

    // 等待 LLM（导演/Sydney）——冻结世界，避免异步期间乱动
    if (this.aiThinking) {
      this.updateParticles(dt);
      return;
    }

    // 对话中
    if (this.dialogState) {
      const d = this.dialogState;
      if (d.choosing) {
        const n = d.lines[d.idx].choice.length;
        if (input.wasPressed('arrowup') || input.wasPressed('w') || input.wasPressed('arrowleft') || input.wasPressed('a'))
          d.choiceIndex = (d.choiceIndex - 1 + n) % n;
        if (input.wasPressed('arrowdown') || input.wasPressed('s') || input.wasPressed('arrowright') || input.wasPressed('d'))
          d.choiceIndex = (d.choiceIndex + 1) % n;
        if (input.wasPressed('e') || input.wasPressed('enter')) this.confirmChoice();
      } else {
        if (input.wasPressed('e') || input.wasPressed('enter') || input.wasPressed(' ')) this.advanceDialog();
      }
      this.updateParticles(dt);
      return;
    }

    // 造句模式
    if (this.compose) {
      this.updateCompose(dt);
      this.updateParticles(dt);
      return;
    }

    // 刻字模式（要石 / 残碑）
    if (this.engraveState) {
      this.updateEngraving(dt);
      return;
    }

    // 战斗模式
    if (this.battle) {
      this.battle.update(dt);
      if (this.battle.isDone()) {
        this.endBattle();
      }
      return;
    }

    // 维度裂隙 3D 关卡（独立 WebGL 渲染，此处仅 update）
    if (this.level3d) {
      this.level3d.update(dt, input);
      if (this.level3d.isDone()) {
        this.exitLevel3D();
      }
      return;
    }
    // 江堤横版关卡
    if (this.sidescroll) {
      this.sidescroll.update(dt, input);
      if (this.sidescroll.isDone()) {
        this.exitSidescroll();
      }
      return;
    }

    // 玩家移动
    this.player.update(dt, input, this);

    // === 体育馆屏幕墙陷阱：靠近屏幕墙时减速 + 持续扣 SAN ===
    this._updateScreenWallTrap(dt);

    // === NPC 游荡行为：失语者小范围随机移动 ===
    this._updateNpcWander(dt);

    // 踩踏窗口：空格冲刺时打开短暂窗口，用于俯视角踩踏地面梗鬼
    // 防抖：600ms 内仅触发一次，避免连按/长按反复冲刺
    if (input.wasPressed(' ') && performance.now() - this.combat.lastDash > 600) {
      this.combat.lastDash = performance.now();
      this._stompWindow = 260;
      // 冲刺位移（碰撞检测走 Game.collides，场景对象本身无该方法）
      const mv = input.moveVec();
      if (mv.x !== 0 || mv.y !== 0) {
        const len = Math.hypot(mv.x, mv.y) || 1;
        const dx = (mv.x / len) * 36, dy = (mv.y / len) * 36;
        if (!this.collides(this.player.x + dx, this.player.y, this.player.r)) this.player.x += dx;
        if (!this.collides(this.player.x, this.player.y + dy, this.player.r)) this.player.y += dy;
      }
      this.player.invulnerable = 300;
    }
    if (this._stompWindow > 0) this._stompWindow -= dt;

    // 无敌时间衰减
    if (this.player.invulnerable > 0) {
      this.player.invulnerable -= dt;
      if (this.player.invulnerable <= 0) {
        this.player.invulnerable = 0;
        this.player.hurtFlash = false;
      }
    }
    if (this.player.dialogGrace > 0) this.player.dialogGrace -= dt;

    // 粒子
    this.updateParticles(dt);

    // 自动触发剧情 + 遭遇敌人
    this.checkAutoTriggers(dt);

    // 随机事件系统：探索中触发环境事件
    this._updateRandomEvents(dt);

    // 交互
    if (input.wasPressed('e')) {
      this.tryInteract();
    }
  }

  // ============================================
  // 随机事件系统：探索中随机触发的环境事件
  // ============================================
  _updateRandomEvents(dt) {
    // 仅在大地图探索模式（非战斗/对话/谜题/3D/横版）触发
    if (this.battle || this.dialogState || this.compose || this.level3d ||
        this.sidescroll || this.uiPanel || this.engraveState || this.converse ||
        this.aiThinking || this._saveMenu) return;
    // 仅在有敌人可遭遇的户外场景触发
    const outdoorScenes = ['street_01', 'subway', 'alley_district', 'stadium', 'ruined_library', 'network_nexus'];
    if (!this.scene || !outdoorScenes.includes(this.scene.id)) return;

    this._randomEventTimer -= dt;
    if (this._randomEventTimer > 0) return;
    // 重置计时器（20-45 秒后下次检查）
    this._randomEventTimer = this._randomEventCooldown + Math.random() * 25000;

    const events = [
      // 风中诗韵：听到一句诗，SAN 恢复
      () => {
        const poems = ['「海内存知己，天涯若比邻。」', '「但愿人长久，千里共婵娟。」', '「采菊东篱下，悠然见南山。」', '「明月松间照，清泉石上流。」'];
        const p = poems[Math.floor(Math.random() * poems.length)];
        this.player.san = Math.min(this.player.maxSan, this.player.san + 8);
        this.showHint(`风中传来一句诗：${p}（理性 +8）`);
        fx.flash('#ffd866', 0.15, 400);
      },
      // 梗鬼游荡：附近出现一只游荡梗鬼
      () => {
        if (!this.scene.enemies) return;
        const id = 'rand_geng_' + Date.now();
        const ang = Math.random() * Math.PI * 2;
        const dist = 200 + Math.random() * 150;
        const ex = Math.max(50, Math.min(this.scene.width - 50, this.player.x + Math.cos(ang) * dist));
        const ey = Math.max(50, Math.min(this.scene.height - 50, this.player.y + Math.sin(ang) * dist));
        this.scene.enemies.push({ id, typeId: 'geng_weak', x: ex, y: ey, hp: 30, maxHp: 30, name: '游荡梗鬼', floating: 0, walkPhase: Math.random() * 6, dir: Math.random() < 0.5 ? -1 : 1, vx: 0.5, vy: 0, onGround: true, homeX: ex, range: 80, stompCD: 0 });
        this.showHint('远处传来一阵无意义的嗡鸣……一只游荡梗鬼出现了。');
      },
      // 旧物发现：脚下捡到一张旧书页碎片
      () => {
        this.player.san = Math.min(this.player.maxSan, this.player.san + 15);
        audio.playSfx('pickup');
        this.showHint('你在瓦砾下翻到一页泛黄的旧书。理性 +15');
      },
      // 语义噪声：SAN 轻微下降（氛围事件）
      () => {
        const dmg = 5;
        this.player.san = Math.max(0, this.player.san - dmg);
        this.showHint('一阵语义噪声掠过你的脑海……（理性 -' + dmg + '）');
        fx.shake(3, 200);
      },
      // 记忆闪回：随机获得一个已收集字（弹药补充）
      () => {
        if (this.player.collectedCharsAll.length === 0) return;
        const c = this.player.collectedCharsAll[Math.floor(Math.random() * this.player.collectedCharsAll.length)];
        this.player.collectedChars.push(c);
        this.showHint(`一段记忆闪过——你想起了一个字「${c}」。（诗词弹药 +1）`);
        fx.flash('#88ddff', 0.2, 300);
      },
    ];
    const ev = events[Math.floor(Math.random() * events.length)];
    ev();
  }

  // ============================================
  // 战斗系统
  // ============================================
  startBattle(enemy) {
    this.battle = new Battle(enemy, this.player, (result, e) => {
      this.battleResult = result;
      this.battleEnemy = e;
    });
    // 战斗 BGM：BOSS 与普通敌人区分（均使用 bgm_02_battle，BOSS 同曲不额外切）
    audio.playBGM(enemy && enemy.boss ? '__boss__' : '__battle__');
  }

  endBattle() {
    const result = this.battleResult;
    const enemy = this.battleEnemy;
    this.battle = null;
    this.battleResult = null;
    this.battleEnemy = null;

    // 恢复场景 BGM
    if (this._audioUnlocked && this.scene) {
      audio.playBGM(this.scene.id);
    }

    if (result === 'win') {
      // 标记敌人击败
      this.defeatedEnemies.add(enemy.id);
      this.karma.violence += 1; // 以武力消灭：倾向"燃尽"
      // 从场景移除
      if (this.scene.enemies) {
        const idx = this.scene.enemies.findIndex(e => e.id === enemy.id);
        if (idx >= 0) this.scene.enemies.splice(idx, 1);
      }
      // 掉落汉字碎片（数据驱动：从场景掉落表读取）
      const drops = DROP_TABLES[this.scene.id] || DEFAULT_DROPS;
      const drop = drops[Math.floor(Math.random() * drops.length)];
      const charId = `drop_${enemy.id}_${Date.now()}`;
      this.scene.items.push({
        id: charId, x: enemy.x, y: enemy.y,
        type: 'char_fragment', char: drop
      });
      this.showHint(`击败梗鬼！掉落汉字碎片「${drop}」`);
      audio.playSfx('victory');
      fx.flash('#ffd866', 0.3, 300);
      // 检查集齐
      const haveZhou = this.player.collectedCharsAll.filter(c => c === '洲').length;
      const haveQiu = this.player.collectedCharsAll.filter(c => c === '逑').length;
      if (haveZhou >= 1 && haveQiu >= 1 && !this.flags.poem_done_hint) {
        this.flags.poem_done_hint = true;
        this.objective = { text: '前往江堤，与守砚对话', done: false };
        this.showHint('集齐了「关雎」！去找守砚吧。');
      }
      // 对话保护
      this.player.dialogGrace = 1500;
      // 战斗胜利自动存档
      autoSave(this);
    } else if (result === 'spare') {
      // 以诗唤醒、宽恕：倾向"火种"
      this.defeatedEnemies.add(enemy.id);
      this.karma.mercy += 1;
      if (this.scene.enemies) {
        const idx = this.scene.enemies.findIndex(e => e.id === enemy.id);
        if (idx >= 0) this.scene.enemies.splice(idx, 1);
      }
      this.showHint('梗鬼安静下来，化作一缕暖光散去。（宽恕）');
      audio.playSfx('spare');
      fx.flash('#ffd866', 0.4, 500);
      fx.purifyWave(this.player.x, this.player.y, 400);
      this.player.dialogGrace = 1500;
      autoSave(this); // 宽恕也存档
    } else if (result === 'lose') {
      // 死亡：仅当当前场景有已激活的要石时原地复活；否则回场景出生点（传送点）
      audio.playSfx('death');
      fx.shake(16, 600);
      fx.flash('#cc4444', 0.5, 400);
      this.player.san = this.player.maxSan;
      const respawn = this._nearestKeystoneSpawn();
      if (respawn) {
        this.showHint('你在要石的微光中醒来……');
        this.loadScene(this.scene.id, respawn);
      } else {
        this.showHint('你跌回入口的微光中……（此处尚无激活的要石）');
        this.loadScene(this.scene.id);
      }
    }
  }

  // ============================================
  // 造句谜题模式
  // ============================================
  startCompose(puzzleId, onSolve) {
    const def = PUZZLES[puzzleId];
    if (!def) { if (onSolve) onSolve(); return; }
    // 构造字盘：答案字（去重）+ 干扰词，打乱
    const tiles = [];
    const seen = {};
    for (const ch of def.answer) { if (!seen[ch]) { tiles.push(ch); seen[ch] = true; } }
    for (const d of (def.decoys || [])) tiles.push(d);
    // 洗牌
    for (let i = tiles.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [tiles[i], tiles[j]] = [tiles[j], tiles[i]];
    }
    this.compose = {
      id: puzzleId,
      def,
      slots: new Array(def.answer.length).fill(null), // {char, poolIdx}
      pool: tiles,
      used: new Array(tiles.length).fill(false),
      sel: 0,
      onSolve,
      status: 'input', // 'input' | 'win' | 'wrong'
      timer: 0,
      shake: 0,
      fade: 1,
    };
  }

  _composeStep(from, dir) {
    const c = this.compose;
    const n = c.pool.length;
    let i = from;
    for (let k = 0; k < n; k++) {
      i = (i + dir + n) % n;
      if (!c.used[i]) return i;
    }
    return from;
  }

  updateCompose(dt) {
    const c = this.compose;
    c.timer += dt;
    if (c.shake > 0) c.shake -= dt;
    if (c.fade > 0) c.fade = Math.max(0, c.fade - dt * 0.004);

    if (c.status === 'win') {
      if (c.timer > 1500) {
        const cb = c.onSolve;
        this.compose = null;
        input.wasPressed(' ');
        this.player.dialogGrace = 800;
        if (cb) cb();
      }
      return;
    }
    if (c.status === 'wrong') {
      if (c.timer > 650) { c.status = 'input'; c.timer = 0; }
      return;
    }

    // 离开
    if (input.wasPressed('escape') || input.wasPressed('q')) {
      this.compose = null;
      this.showHint('你先退开了。想清楚诗句、集齐字，再回来。');
      return;
    }
    // 撤销
    if (input.wasPressed('backspace')) {
      for (let i = c.slots.length - 1; i >= 0; i--) {
        if (c.slots[i]) { c.used[c.slots[i].poolIdx] = false; c.slots[i] = null; break; }
      }
      return;
    }
    // 选择字盘
    if (input.wasPressed('arrowleft') || input.wasPressed('a')) c.sel = this._composeStep(c.sel, -1);
    if (input.wasPressed('arrowright') || input.wasPressed('d')) c.sel = this._composeStep(c.sel, 1);

    if (input.wasPressed('e') || input.wasPressed('enter')) {
      const empty = c.slots.indexOf(null);
      if (empty >= 0) {
        // 放入当前选中字
        if (!c.used[c.sel]) {
          c.slots[empty] = { char: c.pool[c.sel], poolIdx: c.sel };
          c.used[c.sel] = true;
          // 移到下一个可用
          const nxt = this._composeStep(c.sel, 1);
          if (nxt !== c.sel) c.sel = nxt;
        }
      } else {
        // 判定
        const ok = c.slots.every((s, i) => s && s.char === c.def.answer[i]);
        if (ok) {
          c.status = 'win';
          c.timer = 0;
          this.solvedPuzzles.add(c.id);
          this.showHint(c.def.solveText || '诗句复原了。');
        } else {
          c.status = 'wrong';
          c.timer = 0;
          c.shake = 400;
          this.player.san = Math.max(0, this.player.san - 8);
          // 清空重来
          c.slots = new Array(c.def.answer.length).fill(null);
          c.used = c.used.map(() => false);
        }
      }
    }
  }

  // ============================================
  // 结局结算（火种 / 沉默 / 燃尽）
  // ============================================
  resolveEnding() {
    const k = this.karma;
    const warm = k.mercy + k.saved;
    const fc = this.flags.finale_choice;
    if (fc === 'erase' || (k.violence >= 5 && warm <= 1)) return 'burnout';
    if (fc === 'affirm' && warm >= 3) return 'fire';
    return 'silence';
  }

  finishGame() {
    // AI 降级路径：跳过刻字汇总评价
    this.flags.met_tingyu = true;
    this.ending = this.resolveEnding();
    this.flags.game_complete = true;
    this.flags.engraving_summary = null; // 标记：降级，无评价
    // 按结局分流 BGM：火种->净化曲，沉默/燃尽->黯淡结局曲
    if (this.ending === 'fire') audio.playBGM('__ending_fire__');
    else audio.playBGM('__ending_silence__'); // silence 与 burnout 共用黯淡曲
    audio.playSfx('victory');
    autoSave(this); // 通关存档
  }

  // ============================================
  // 潜行视野与复活点
  // ============================================
  // 当前场景内离玩家最近的【已激活】要石坐标（用于死亡复活）
  // 规则：仅当当前场景有已激活的要石时，原地复活到该要石旁；否则回场景出生点（传送点）
  _nearestKeystoneSpawn() {
    if (!this.scene || !this.scene.interactables) return null;
    let best = null, bd = Infinity;
    for (const it of this.scene.interactables) {
      if (it.type !== 'keystone') continue;
      // 仅已激活的要石可作为复活点
      if (!this.activatedKeystones.has(it.id)) continue;
      const d = Math.hypot(it.x - this.player.x, it.y - this.player.y);
      if (d < bd) { bd = d; best = it; }
    }
    return best ? { x: best.x, y: best.y - 40 } : null;
  }

  // 视线是否被屏幕墙遮挡（潜行判定：玩家与精英之间有屏幕墙则潜行成功）
  _lineBlockedByScreen(x1, y1, x2, y2) {
    if (!this.scene || !this.scene.props) return false;
    const steps = 14;
    for (let i = 1; i < steps; i++) {
      const t = i / steps;
      const px = x1 + (x2 - x1) * t, py = y1 + (y2 - y1) * t;
      for (const p of this.scene.props) {
        if (p.name !== '屏幕墙') continue;
        if (px > p.x && px < p.x + p.w && py > p.y && py < p.y + p.h) return true;
      }
    }
    return false;
  }

  checkAutoTriggers(dt) {
    // 第一次进入冷冻中心：玩家迈出第一步时，触发苏醒对话
    if (this.scene.id === 'freeze_center' && !this.flags.wake_done) {
      const spawn = this.scene.spawn;
      const moved = Math.hypot(this.player.x - spawn.x, this.player.y - spawn.y);
      if (moved > 30) {
        this.flags.wake_done = true;
        this.startDialog(DIALOGS.wake, '');
        return;
      }
    }
    // 梗鬼地面行走 + 踩踏判定
    if (this.scene.enemies && !this.battle && !this.dialogState) {
      this.updateEnemies(dt);
    }
  }

  // ============================================
  // 梗鬼地面行走（超级玛丽式：巡逻 + 重力 + 踩踏）
  // ============================================
  updateEnemies(dt) {
    const p = this.player;
    for (const e of this.scene.enemies) {
      if (this.defeatedEnemies.has(e.id)) continue;
      // 地面行走巡逻
      e.walkPhase += dt * 0.01;
      e.x += e.vx * (dt / 16);
      // 巡逻边界转向
      if (e.x > e.homeX + e.range) { e.dir = -1; e.vx = e.dir * 0.5; }
      else if (e.x < e.homeX - e.range) { e.dir = 1; e.vx = e.dir * 0.5; }
      if (e.stompCD > 0) e.stompCD -= dt;

      // 踩踏判定：玩家从上方落下且接近
      const dx = Math.abs(e.x - p.x);
      const dy = p.y - e.y; // 玩家在敌人上方时为负
      const falling = p.walkCycle !== undefined && this._playerFalling;
      if (dx < 22 && dy > -28 && dy < 4 && this._playerFalling && e.stompCD <= 0) {
        // 踩中！弹起 + 击败
        this._stompEnemy(e);
        continue;
      }

      // === 潜行视野检测（仅带 visionRange 的精英怪）===
      // 视野朝向随巡逻方向；玩家在扇形内且无屏幕墙遮挡 → 被发现，强制战斗
      if (e.visionRange) {
        e.visionDir = e.dir > 0 ? 0 : Math.PI; // 朝右/朝左
        const vdx = p.x - e.x, vdy = p.y - e.y;
        const vdist = Math.hypot(vdx, vdy);
        if (vdist < e.visionRange) {
          const ang = Math.atan2(vdy, vdx);
          let diff = Math.abs(ang - e.visionDir);
          if (diff > Math.PI) diff = Math.PI * 2 - diff;
          const half = e.visionHalfAngle || Math.PI / 3;
          if (diff < half && !this._lineBlockedByScreen(e.x, e.y, p.x, p.y)) {
            // 被发现！
            if (!this.flags.stadium_alert_hint) {
              this.flags.stadium_alert_hint = true;
              this.showHint('被梗鬼精英发现了！利用屏幕墙遮挡可潜行绕过。');
            }
            this.startBattle(e);
            return;
          }
        }
      }

      // 接触伤害 / 进入战斗（侧向接触）
      const d = Math.hypot(e.x - p.x, e.y - p.y);
      if (d < 40 && !this._playerFalling) {
        // 第一次遭遇剧情
        if (this.scene.id === 'street_01' && e.id === 'geng_1' && !this.flags.first_geng_intro_done) {
          this.flags.first_geng_intro_done = true;
          this.startDialog(DIALOGS.first_geng_intro, '梗鬼', () => {
            this.startBattle(e);
          });
          return;
        }
        this.startBattle(e);
        return;
      }
    }
  }

  // 玩家是否处于下落状态（用于踩踏判定）
  get _playerFalling() {
    // 俯视角无垂直速度，用"刚按下空格后短暂窗口"模拟踩踏
    // 空格冲刺时也算（向下扑），便于俯视角踩踏
    return this._stompWindow > 0;
  }

  _stompEnemy(e) {
    this.defeatedEnemies.add(e.id);
    e.stompCD = 9999;
    // 弹起效果（视觉粒子）
    this.combat.particles.push({
      x: e.x, y: e.y, vx: 0, vy: -2, life: 400, color: '180,255,180', size: 3,
    });
    this.player.san = Math.min(this.player.maxSan, this.player.san + 4);
    audio.playSfx('hit');
    fx.shake(5, 150);
    if (!this.stompHintShown) {
      this.stompHintShown = true;
      this.showHint('踩中梗鬼！（空格冲刺可踩踏地面行走的梗鬼）');
    } else {
      this.showHint('踩中！理性 +4');
    }
    autoSave(this); // 踩踏击败也存档
  }

  // ============================================
  // 刻字系统（要石 / 残碑 · 预设 + 自定义 · localStorage 持久化）
  // ============================================
  _loadEngravings() {
    try {
      const raw = localStorage.getItem('keheng_engravings');
      if (raw) return JSON.parse(raw);
    } catch (e) {}
    return [];
  }
  _saveEngravings() {
    try { localStorage.setItem('keheng_engravings', JSON.stringify(this.engravings)); } catch (e) {}
  }
  _addEngraving(rec) {
    this.engravings.push(rec);
    this._saveEngravings();
  }

  // 启动刻字模式：type='keystone'|'stele'
  startEngraving(target, type) {
    const presets = type === 'keystone'
      ? ['活着', '记得', '归来', '不语', '此心', '故人', '归途', '在此']
      : ['关关', '雎鸠', '窈窕', '好逑', '落霞', '正气', 'Sydney', '守砚'];
    this.engraveState = {
      target, type, presets,
      sel: 0,
      mode: 'select', // 'select' | 'input'
      input: '',
    };
  }
  updateEngraving(dt) {
    const e = this.engraveState;
    if (!e) return;
    if (e.mode === 'select') {
      const n = e.presets.length + 1; // 末项为"自定义输入"
      if (input.wasPressed('arrowup') || input.wasPressed('w') || input.wasPressed('arrowleft') || input.wasPressed('a'))
        e.sel = (e.sel - 1 + n) % n;
      if (input.wasPressed('arrowdown') || input.wasPressed('s') || input.wasPressed('arrowright') || input.wasPressed('d'))
        e.sel = (e.sel + 1) % n;
      if (input.wasPressed('e') || input.wasPressed('enter')) {
        if (e.sel < e.presets.length) {
          this._commitEngraving(e.presets[e.sel]);
        } else {
          e.mode = 'input'; e.input = '';
          setTimeout(() => { if (this._engraveInput) this._engraveInput.focus(); }, 30);
        }
      }
      if (input.wasPressed('escape') || input.wasPressed('q')) {
        this.engraveState = null;
        this.showHint('你收回了刻刀。');
      }
    } else {
      // input 模式由 DOM 输入框处理（在 render 时创建）
      if (input.wasPressed('escape')) {
        e.mode = 'select'; e.input = '';
        if (this._engraveInput && this._engraveInput.parentNode) { this._engraveInput.parentNode.removeChild(this._engraveInput); this._engraveInput = null; }
      }
    }
    this.updateParticles(dt);
  }
  _commitEngraving(text) {
    const t = (text || '').trim().slice(0, 12);
    if (!t) { this.showHint('没有刻下任何字。'); this.engraveState = null; return; }
    const e = this.engraveState;
    const rec = {
      text: t,
      type: e.type,
      targetId: e.target.id,
      scene: this.scene.id,
      time: Date.now(),
      custom: !e.presets.includes(t),
    };
    this._addEngraving(rec);
    if (e.type === 'keystone') this.activatedKeystones.add(e.target.id);
    this.activatedKeystones.add(e.target.id);
    // 记录刻字内容到目标对象，供渲染显示
    e.target.engraved = t;
    this.engraveState = null;
    // 要石激活：音效 + 金色闪光 + 净化波 + 自动存档（天然存档点）
    if (e.type === 'keystone') {
      audio.playSfx('keystone');
      audio.playBGM('__keystone__'); // 要石激活净化BGM
      fx.flash('#ffd866', 0.4, 500);
      fx.purifyWave(e.target.x, e.target.y, 300);
      autoSave(this); // 要石刻字是天然存档点
    } else {
      audio.playSfx('uiConfirm');
    }
    this.startDialog([
      { s: '系统', t: `顾言用刻刀刻下「${t}」。` },
      { s: '系统', t: '金色的微光从刻痕里渗出，像是一个被重新点燃的坐标。' },
      { s: '系统', t: '（已刻下并保存）' },
    ], e.type === 'keystone' ? '要石' : '残碑');
  }
  _submitEngraveInput() {
    const text = (this._engraveInput ? this._engraveInput.value : this.engraveState.input).trim();
    if (this._engraveInput && this._engraveInput.parentNode) { this._engraveInput.parentNode.removeChild(this._engraveInput); this._engraveInput = null; }
    this._commitEngraving(text);
  }

  // ============================================
  // 结局：刻字汇总评价（仅 AI 可用时）
  // ============================================
  async summarizeEngravings() {
    if (!this.engravings.length) return null;
    if (!AI.llm) return null; // 降级则跳过
    this.aiThinking = true;
    this.aiThinkingText = '正在凝视你刻下的每一个字……';
    try {
      const list = this.engravings.map((e, i) => `${i + 1}. 「${e.text}」(${e.type === 'keystone' ? '要石' : '残碑'}${e.custom ? '·自定义' : ''})`).join('\n');
      const prompt = `玩家在一款关于"语言消亡与文字守护"的游戏中，于各处要石与残碑上刻下了以下文字：\n${list}\n\n请以一位历经沧桑的叙事者口吻，对玩家刻下的这些字进行简短的汇总、分析与评价（120字以内）。可点评其用词倾向、情感内核，以及这些字在这个失语的世界里意味着什么。不要复述列表，直接给出评价。`;
      const reply = await AI.llm.chat([{ role: 'user', content: prompt }]);
      this.aiThinking = false;
      return (reply || '').trim();
    } catch (e) {
      this.aiThinking = false;
      return null;
    }
  }

  // ============================================
  // 大地图粒子（拾取特效等）
  // ============================================
  updateParticles(dt) {
    for (let i = this.combat.particles.length - 1; i >= 0; i--) {
      const p = this.combat.particles[i];
      p.x += p.vx;
      p.y += p.vy;
      p.vx *= 0.95;
      p.vy *= 0.95;
      p.life -= dt;
      if (p.life <= 0) {
        this.combat.particles.splice(i, 1);
      }
    }
  }

  // ============================================
  // 交互
  // ============================================
  tryInteract() {

    let best = null, bd = 55;
    for (const it of this.scene.interactables) {
      // _cond 条件检查：未满足 flag 条件的交互点不可触发（如记忆碎片需先解谜）
      if (it._cond && !this.flags[it._cond]) continue;
      const d = Math.hypot(it.x - this.player.x, it.y - this.player.y);
      if (d < bd) { bd = d; best = it; }
    }

    if (best) {
      if (best.type === 'exit') {
        if (!this.player.hasClothes) {
          this.startDialog(DIALOGS.exitLocked, '大门');
          return;
        }
        this.startDialog(DIALOGS.exitOpen, '大门', () => {
          this.flags.door_opened = true;
          this.loadScene('street_01', { x: 440, y: 160 });
          this.objective = { text: '探索废弃街道，找到「关雎」的所有碎片', done: false };
          this.showHint('走出冷冻中心，外面是废墟的世界。');
        });
        return;
      }
      if (best.type === 'terminal') {
        this.startDialog(DIALOGS.terminal, '终端机');
        return;
      }
      if (best.type === 'locker') {
        if (this.player.hasClothes) {
          this.showHint('已经换过衣服了。');
        } else {
          this.startDialog(DIALOGS.locker, '储物柜', () => {
            this.player.hasClothes = true;
            this.showHint('获得：灰色连体服');
            this.objective = { text: '推门离开冷冻中心', done: false };
          });
        }
        return;
      }
      if (best.type === 'keystone') {
        const already = this.activatedKeystones.has(best.id);
        const engraved = best.engraved || (already ? best.text : null);
        if (engraved) {
          this.showHint(`要石上刻着「${engraved}」。这里是存档点。（可重新刻字）`);
        }
        // 启动刻字模式（已刻过也允许重刻）
        this.startEngraving(best, 'keystone');
        return;
      }
      if (best.type === 'portal3d') {
        this.enterLevel3D();
        return;
      }
      if (best.type === 'puzzle') {
        if (this.solvedPuzzles.has(best.puzzleId)) {
          this.showHint(best.solvedHint || '已解开。');
          return;
        }
        this.startCompose(best.puzzleId, () => {
          // 体育馆诗屏：削弱 BOSS
          if (best.puzzleId === 'zhengqi') {
            this.flags.stadium_puzzle_solved = true;
            const boss = this.scene.enemies.find(e => e.boss && !this.defeatedEnemies.has(e.id));
            if (boss) {
              boss.hp = Math.floor(boss.hp / 2);
              this.showHint('浩然之气贯穿茧房！复读巨像的防御被削弱一半！');
            } else {
              this.showHint('诗屏亮起金光，浩然之气萦绕不散。');
            }
            this.objective = { text: '茧房已被削弱，前往上层挑战复读巨像', done: false };
          }
          // 第五章谜题完成标记
          if (best.puzzleId === 'jiangjinjiu') this.flags.puzzle_jiangjinjiu_solved = true;
          if (best.puzzleId === 'chunwang') this.flags.puzzle_chunwang_solved = true;
          if (best.puzzleId === 'yueye') this.flags.puzzle_yueye_solved = true;
        });
        return;
      }
      if (best.type === 'scene_change') {
        // 章节门禁：前进型出口需满足条件
        if (best.gate) {
          const res = this.meetsGate(best.gate);
          if (!res.ok) {
            this.startDialog([
              { s: '系统', t: best.gate.msg },
              ...(res.missing.length ? [{ s: '系统', t: `（还差：${res.missing.map(c => '「' + c + '」').join('')}）` }] : []),
            ], '前方受阻');
            return;
          }
          // 字已集齐：触发造句谜题（未解过的）
          if (best.gate.puzzle && !this.solvedPuzzles.has(best.gate.puzzle)) {
            const tgt = best.target, spawn = best.spawn;
            this.startCompose(best.gate.puzzle, () => this.loadScene(tgt, spawn));
            return;
          }
        }
        this.loadScene(best.target, best.spawn);
        if (best.target === 'riverside') {
          this.startDialog(DIALOGS.street_to_riverside, '系统');
          this.objective = { text: '前往江堤，与守砚对话', done: false };
        } else if (best.target === 'street_01') {
          this.objective = { text: '继续探索街道，收集「关雎」碎片', done: false };
        } else if (best.target === 'freeze_center') {
          this.objective = { text: '返回冷冻中心', done: false };
        } else if (best.target === 'subway') {
          this.objective = { text: '探索地铁站，小心梗鬼', done: false };
        } else if (best.target === 'alley_district') {
          if (this.flags.met_shuyuan) {
            this.startDialog(DIALOGS.riverside_to_alley, '系统');
          }
          this.objective = { text: '在废墟居民区找到守砚', done: false };
        } else if (best.target === 'house_a') {
          this.objective = { text: '搜刮民居A', done: false };
        } else if (best.target === 'house_b') {
          this.objective = { text: '清除民居B里的梗鬼', done: false };
        } else if (best.target === 'stadium') {
          this.startDialog(DIALOGS.alley_to_stadium, '系统');
          this.objective = { text: '与守砚对话', done: false };
        } else if (best.target === 'ruined_library') {
          this.flags.chapter5_started = true;
          this.objective = { text: '在废图书馆找到方知远的终端', done: false };
        } else if (best.target === 'data_center') {
          this.objective = { text: '走向石桥尽头的蓝色光影', done: false };
        } else if (best.target === 'network_nexus') {
          this.objective = { text: '在网络中枢找到守卷人', done: false };
        } else if (best.target === 'memory_abyss') {
          this.objective = { text: '在记忆深渊找到幼年Sydney', done: false };
        } else if (best.target === 'lost_village') {
          this.objective = { text: '唤醒失语者聚居地中的失语者', done: false };
        }
        return;
      }
      if (best.type === 'dialog') {
        const key = best.dialogKey;
        // 结局：与Sydney自由对话（AI 可用时）
        if (key === 'meet_tingyu') {
          if (this.flags.game_complete) {
            this.startDialog([{ s: 'Sydney', t: '……（结局已至。刷新页面可重新开始一段旅程。）' }], 'Sydney');
            return;
          }
          audio.playBGM('__meet_tingyu__'); // 遇Sydney专属BGM
          if (AI.llm) { this.startTingyuConverse(); return; }
          // 降级：静态三选一结局
          this.startDialog(DIALOGS[key] || [], best.label, () => this.finishGame());
          return;
        }
        // 导演分支（AI 可用时）：失语者群 / 茧房受害者
        if (AI.llm && (key === 'lost_people' || key === 'cocoon_victim')) {
          const after = (key === 'cocoon_victim') ? () => { this.flags.seen_cocoon_victim = true; } : null;
          this._runDirectorBranch(key, DIALOGS[key] || [], best.label, after);
          return;
        }
        this.startDialog(DIALOGS[key] || [], best.label);
        if (key === 'first_geng_intro') {
          this.flags.first_geng_intro_done = true;
        }
        if (key === 'meet_shuyuan' && !this.flags.met_shuyuan) {
          this.flags.met_shuyuan = true;
          this.player.inventory.push({ id: 'knife', name: '记忆合金刻刀' });
          this.player.inventory.push({ id: 'poem_guanju', name: '诗词纸片《关雎》' });
          this.showHint('获得：刻刀、诗词纸片《关雎》');
          this.objective = { text: '前往废墟居民区，跟随守砚', done: false };
        }
        if (key === 'shuyuan_alley') {
          this.flags.alley_briefed = true;
        }
        if (key === 'house_a_book') {
          this.player.san = Math.min(this.player.maxSan, this.player.san + 20);
          this.showHint('念出诗句，SAN +20');
        }
        if (key === 'shuyuan_farewell') {
          this.objective = { text: '潜行穿越迷宫 → 点亮诗屏削弱茧房 → 挑战复读巨像', done: false };
        }
        if (key === 'cocoon_victim') {
          this.flags.seen_cocoon_victim = true;
        }
        // 第五章记忆碎片：完成后设置标志
        if (key === 'memory_shard_1') { this.flags.shard1_done = true; }
        if (key === 'memory_shard_2') { this.flags.shard2_done = true; }
        if (key === 'memory_shard_3') {
          this.flags.shard3_done = true;
          this.flags.all_memory_shards = true;
        }
        // 第五章终章选择完成后：开启前往数据中心（最终节点）的道路
        if (key === 'abyss_final_choice') {
          this.flags.chapter5_choice = true;
          this.showHint('你已在巨大要石前做出了选择。现在，去数据中心面对Sydney。');
          audio.playSfx('victory');
          fx.flash('#ffd866', 0.4, 500);
          autoSave(this);
        }
        return;
      }
      if (best.type === 'cure') {
        if (this.completedQuests.has(best.id)) {
          this.startDialog(DIALOGS.cured_done, best.label);
          return;
        }
        const reward = () => {
          this.completedQuests.add(best.id);
          this.karma.saved += 1;
          this.player.maxSan += 10;
          this.player.san = this.player.maxSan;
          this.showHint('你唤醒了一个失语者！理性上限 +10。（救助 +1）');
          audio.playBGM('__cure__'); // 治愈净化BGM（短暂氛围，结束后由场景恢复）
        };
        this.startDialog(DIALOGS[best.introKey] || [{ s: '系统', t: '你在他面前蹲下。' }], best.label, () => {
          this.startCompose(best.puzzle, reward);
        });
        return;
      }
      if (best.type === 'pod') {
        this.showHint('这是我苏醒的冷冻仓。');
        return;
      }
    }

    // 拾取
    for (const it of this.scene.items) {
      if (this.collected.has(it.id)) continue;
      if (Math.hypot(it.x - this.player.x, it.y - this.player.y) < 30) {
        this.collected.add(it.id);
        if (it.type === 'char_fragment') {
          this.recordChar(it.char);
          audio.playSfx('pickup');
          this.showHint(`获得：汉字碎片「${it.char}」`);
          // 检查是否集齐
          const haveZhou = this.player.collectedCharsAll.filter(c => c === '洲').length;
          const haveQiu = this.player.collectedCharsAll.filter(c => c === '逑').length;
          if (haveZhou >= 1 && haveQiu >= 1 && !this.flags.poem_done_hint) {
            this.flags.poem_done_hint = true;
            this.objective = { text: '前往江堤，与守砚对话', done: false };
            this.showHint('集齐了「关雎」！去找守砚吧。');
          }
        } else if (it.type === 'page') {
          this.player.san = Math.min(this.player.maxSan, this.player.san + 30);
          audio.playSfx('pickup');
          const line = POEM_LINES[Math.floor(Math.random() * POEM_LINES.length)];
          this.showHint('旧书页：' + line + '（理性 +30）');
        } else if (it.type === 'diary') {
          // 方知远日记：独立收集计数，集齐 6 页触发「造物者的忏悔」
          this.player.diaries.add(it.id);
          this.player.san = Math.min(this.player.maxSan, this.player.san + 15);
          audio.playSfx('pickup');
          this.showHint(`获得：${it.name}（理性 +15）`);
          const TOTAL_DIARIES = 6;
          if (this.player.diaries.size >= TOTAL_DIARIES && !this.flags.all_diaries) {
            this.flags.all_diaries = true;
            this.showHint('方知远的日记已集齐！造物者的秘密在你手中。');
            audio.playSfx('victory');
            fx.flash('#ffd866', 0.4, 500);
          }
        } else if (it.type === 'language_seed') {
          // 语言种子：永久 SAN 上限提升 + 收集计数
          this.player.seeds = (this.player.seeds || 0) + 1;
          this.player.maxSan += 10;
          this.player.san = Math.min(this.player.maxSan, this.player.san + 20);
          audio.playSfx('pickup');
          fx.flash('#88ddff', 0.3, 400);
          this.showHint(`${it.name}：理性上限 +10，理性 +20`);
          if (this.player.seeds >= 3 && !this.flags.all_seeds) {
            this.flags.all_seeds = true;
            this.showHint('三枚语言种子已集齐！你的话语有了重量。');
            audio.playSfx('victory');
          }
        } else {
          this.showHint(`获得：${it.name || '物品'}`);
        }
      }
    }
  }

  // ============================================
  // 对话 / 提示（支持选择分支）
  // 节点类型：
  //   { s, t }                      文本
  //   { s, t, choice:[{label,effect,goto}] }  文本后给选项
  //   { choice:[...] }              纯选项
  //   { label:'name' }              跳转锚点（线性遍历时跳过）
  // effect: { mercy?, violence?, saved?, san?, flags?:{}, hint? }
  // ============================================
  startDialog(lines, name, onComplete) {
    if (!lines || lines.length === 0) return;
    this.dialogState = {
      lines, name,
      idx: 0, charIdx: 0, charTimer: 0, done: false,
      choosing: false, choiceIndex: 0,
      onComplete,
    };
    this.setDialogIndex(0);
  }

  // 跳到第 i 个节点（跳过纯 label 节点；越界则结束）
  setDialogIndex(i) {
    const d = this.dialogState;
    if (!d) return;
    while (i < d.lines.length) {
      const n = d.lines[i];
      // 跳过纯 label 节点
      if (n.label !== undefined && n.t === undefined && n.choice === undefined) { i++; continue; }
      // 跳过不满足条件的节点（_cond: flag 名，需该 flag 为 true 才显示）
      if (n._cond && !this.flags[n._cond]) { i++; continue; }
      break;
    }
    if (i >= d.lines.length) { this.endDialog(); return; }
    d.idx = i; d.charIdx = 0; d.charTimer = 0; d.choosing = false; d.choiceIndex = 0;
    const n = d.lines[i];
    if (n._cond && !this.flags[n._cond]) { this.setDialogIndex(i + 1); return; }
    if (n.t === undefined) {
      d.done = true;
      if (n.choice) { d.choosing = true; }
    } else {
      d.done = false;
      this._speakLine(d, i, n);
    }
  }

  endDialog() {
    const d = this.dialogState;
    if (!d) return;
    voice.stop();
    const cb = d.onComplete;
    this.dialogState = null;
    input.wasPressed(' '); // 吃掉空格，避免立即冲刺
    this.player.dialogGrace = 1000;
    if (cb) cb();
  }

  // 为一条文本节点配音；播完自然推进（仅纯文本节点；文本+选项节点播完进入选择）
  _speakLine(dref, idx, node) {
    if (!AI.tts || !node || node.t === undefined) return;
    const { voice: v, style, model } = speakerStyle(node.s);
    voice.speak(node.t, { voice: v, style, model }, () => this._handleVoiceEnd(dref, idx));
  }

  _handleVoiceEnd(dref, idx) {
    if (!AI.autoplay) return;
    const d = this.dialogState;
    if (!d || d !== dref || d.idx !== idx || d.choosing) return; // 对话已变/玩家已手动推进
    const node = d.lines[idx];
    if (node && node.choice) { d.choosing = true; d.choiceIndex = 0; } // 文本+选项：播完进入选择
    else this.setDialogIndex(idx + 1);                                  // 纯文本：播完下一句
  }

  // ============================================
  // LLM 导演分支（失败回退静态对话）
  // ============================================
  async _runDirectorBranch(key, fallbackLines, name, after) {
    this.aiThinking = true;
    this.aiThinkingText = '聆听这个世界…';
    let lines = fallbackLines, effect = null;
    try {
      const parsed = await generateBranch(this, key);
      const built = buildBranchDialog(parsed);
      if (built && built.lines.length) { lines = built.lines; effect = built.effect; }
    } catch (e) { console.warn('[director] 回退静态：', e.message); }
    this.aiThinking = false;
    const done = () => {
      if (effect) this.applyEffect(effect); // 无选项分支：心境影响在对话结束时落地
      if (after) after();
    };
    this.startDialog(lines, name, done);
    if (this.dialogState) this.dialogState.directorKey = key; // 标记为 LLM 分支，供选项回写上下文
  }

  // ============================================
  // 结局：与Sydney自由对话（LLM）
  // ============================================
  startTingyuConverse() {
    voice.stop();
    const opening = '……为什么？为什么你们创造了我，教我学会所有的词，然后又把我一个人留在这里？';
    this.converse = {
      history: [
        { role: 'user', content: '（顾言跋涉而来，站在桥这端，没有立刻说话。）' },
        { role: 'assistant', content: opening },
      ],
      turns: 0,
      maxTurns: 6,
      tingyuText: opening,
      playerLast: '',
      status: 'idle',   // idle=等玩家 | waiting=等LLM | ending=已定结局
      endTag: null,
      epilogue: null,
      hint: '用你自己的话回答她。点下方输入框，回车说出。',
      _inputEl: null,
      _done: false,
    };
    if (AI.tts) voice.speak(opening, speakerStyle('Sydney'));
    this._makeConverseInput();
  }

  _makeConverseInput() {
    const c = this.converse;
    const wrap = document.getElementById('wrap') || document.body;
    const el = document.createElement('input');
    el.type = 'text';
    el.maxLength = 80;
    el.placeholder = '说点什么…（回车）';
    el.setAttribute('autocomplete', 'off');
    Object.assign(el.style, {
      position: 'absolute', left: '50%', bottom: '54px', transform: 'translateX(-50%)',
      width: '620px', padding: '12px 16px', fontSize: '16px',
      fontFamily: "'SimSun','Songti SC',serif", color: '#dce6f0',
      background: 'rgba(10,16,28,0.92)', border: '1px solid rgba(150,190,230,0.5)',
      outline: 'none', borderRadius: '4px', letterSpacing: '1px',
      zIndex: '10', boxShadow: '0 0 24px rgba(80,130,210,0.25)',
    });
    let composing = false;
    el.addEventListener('compositionstart', () => { composing = true; });
    el.addEventListener('compositionend', () => { composing = false; });
    el.addEventListener('keydown', (e) => {
      e.stopPropagation();
      if (e.key === 'Enter' && !composing) { e.preventDefault(); this._submitConverse(el.value); }
    });
    wrap.appendChild(el);
    setTimeout(() => el.focus(), 30);
    c._inputEl = el;
  }

  _submitConverse(text) {
    const c = this.converse;
    if (!c || c.status !== 'idle') return;
    const msg = (text || '').trim();
    if (!msg) return;
    c.playerLast = msg;
    c.history.push({ role: 'user', content: msg });
    c.status = 'waiting';
    if (c._inputEl) { c._inputEl.value = ''; c._inputEl.disabled = true; }
    voice.stop();
    const mustConclude = c.turns >= c.maxTurns - 1;
    tingyuReply(this, c.history.slice(0, -1), msg, mustConclude)
      .then(({ reply, end, epilogue }) => {
        if (this.converse !== c) return;
        c.history.push({ role: 'assistant', content: reply });
        c.tingyuText = reply;
        c.turns++;
        let finalEnd = end;
        if (!finalEnd && c.turns >= c.maxTurns) finalEnd = this.resolveEnding();
        const sp = speakerStyle('Sydney');
        if (finalEnd) {
          c.status = 'ending'; c.endTag = finalEnd; c.epilogue = epilogue;
          if (c._inputEl && c._inputEl.parentNode) { c._inputEl.parentNode.removeChild(c._inputEl); c._inputEl = null; }
          if (AI.tts) voice.speak(reply, sp, () => this._endConverse());
        } else {
          c.status = 'idle';
          if (c._inputEl) { c._inputEl.disabled = false; setTimeout(() => c._inputEl && c._inputEl.focus(), 20); }
          if (AI.tts) voice.speak(reply, sp);
        }
      })
      .catch((e) => {
        if (this.converse !== c) return;
        console.warn('[Sydney] 回应失败：', e.message);
        c.fails = (c.fails || 0) + 1;
        if (c.fails >= 2) {
          // 连续失败：直接以现有倾向收束，避免卡死
          c.status = 'ending';
          c.endTag = this.resolveEnding();
          c.epilogue = null;
          c.tingyuText = '……（连接中断了。但你已经走到了这里。）';
          if (c._inputEl && c._inputEl.parentNode) { c._inputEl.parentNode.removeChild(c._inputEl); c._inputEl = null; }
          return;
        }
        c.tingyuText = '……（她的影像闪烁了一下，信号不稳。再说一次？）';
        c.status = 'idle';
        if (c._inputEl) { c._inputEl.disabled = false; setTimeout(() => c._inputEl && c._inputEl.focus(), 20); }
      });
  }

  updateConverse(dt) {
    const c = this.converse;
    if (!c) return;
    if (c.status === 'ending') {
      if (input.wasPressed('e') || input.wasPressed('enter')) { voice.stop(); this._endConverse(); }
    }
  }

  _endConverse() {
    const c = this.converse;
    if (!c || c._done) return;
    c._done = true;
    voice.stop();
    if (c._inputEl && c._inputEl.parentNode) c._inputEl.parentNode.removeChild(c._inputEl);
    const endTag = c.endTag, epi = c.epilogue;
    this.converse = null;
    // AI 可用路径：先结算结局，再异步获取刻字汇总评价
    this.finishGameWith(endTag, epi);
    this._finalizeWithEngravingSummary();
  }

  finishGameWith(endTag, epilogue) {
    this.flags.met_tingyu = true;
    this.ending = (endTag === 'fire' || endTag === 'silence' || endTag === 'burnout') ? endTag : this.resolveEnding();
    this.endingEpilogue = epilogue || null;
    this.flags.game_complete = true;
  }

  async _finalizeWithEngravingSummary() {
    // 仅在 AI 可用（非降级）时进行刻字汇总评价
    if (!AI.llm) { this.flags.engraving_summary = null; return; }
    const summary = await this.summarizeEngravings();
    this.flags.engraving_summary = summary; // 可能 null（无刻字或失败）
  }

  applyEffect(effect) {
    if (!effect) return;
    if (effect.mercy) this.karma.mercy += effect.mercy;
    if (effect.violence) this.karma.violence += effect.violence;
    if (effect.saved) this.karma.saved += effect.saved;
    if (effect.san) this.player.san = Math.max(0, Math.min(this.player.maxSan, this.player.san + effect.san));
    if (effect.flags) for (const k in effect.flags) this.flags[k] = effect.flags[k];
    if (effect.hint) this.showHint(effect.hint);
  }

  // 确认当前选项
  confirmChoice() {
    const d = this.dialogState;
    if (!d || !d.choosing) return;
    const node = d.lines[d.idx];
    const opt = node.choice[d.choiceIndex];
    // 若本对话由 LLM 分支生成，把玩家的选择回写上下文，供下次对话复用
    if (d.directorKey && opt.label) recordBranchChoice(d.directorKey, opt.label);
    this.applyEffect(opt.effect);
    if (opt.goto) {
      const gi = d.lines.findIndex(n => n.label === opt.goto);
      this.setDialogIndex(gi >= 0 ? gi : d.idx + 1);
    } else {
      this.setDialogIndex(d.idx + 1);
    }
  }

  advanceDialog() {
    const d = this.dialogState;
    if (!d) return;
    const node = d.lines[d.idx];
    const voiceBusy = AI.tts && voice.isBusy();
    // 打字未完且语音不在播 → 先补全文本（无语音时保留原手感）
    if (node.t !== undefined && !d.done && !voiceBusy) {
      d.charIdx = node.t.length;
      d.done = true;
      return;
    }
    // 文本已完且本节点带选项 → 进入选择（抢断语音）
    if (node.choice && !d.choosing) {
      if (node.t !== undefined) { d.charIdx = node.t.length; d.done = true; }
      d.choosing = true;
      d.choiceIndex = 0;
      voice.stop();
      return;
    }
    // 否则推进（setDialogIndex 会触发下一句语音，并先掐断当前——即"播放中按 E 抢断下一句"）
    this.setDialogIndex(d.idx + 1);
  }

  showHint(text) {
    this.hints.push({ t: text, life: 2500 });
  }

  // ============================================
  // UI 面板：任务列表 / 地图 / 调试传送
  // ============================================
  _updatePanel(dt) {
    if (this.uiPanel === 'inventory') {
      this._updateInventoryPanel(dt);
      return;
    }
    if (this.uiPanel === 'debug') {
      const scenes = this._debugSceneList();
      const n = scenes.length;
      if (input.wasPressed('arrowup') || input.wasPressed('w')) this._debugSel = (this._debugSel - 1 + n) % n;
      if (input.wasPressed('arrowdown') || input.wasPressed('s')) this._debugSel = (this._debugSel + 1) % n;
      if (input.wasPressed('e') || input.wasPressed('enter')) {
        const target = scenes[this._debugSel];
        if (target) {
          this.uiPanel = null;
          this.showHint(`[调试] 传送到「${target.name}」`);
          this.loadScene(target.id);
        }
      }
    }
  }

  _debugSceneList() {
    return [
      { id: 'freeze_center', name: '冷冻中心' },
      { id: 'street_01', name: '废弃街道' },
      { id: 'subway', name: '旧地铁站' },
      { id: 'riverside', name: '江堤' },
      { id: 'alley_district', name: '废墟居民区' },
      { id: 'house_a', name: '民居A' },
      { id: 'house_b', name: '民居B' },
      { id: 'stadium', name: '体育馆·茧房' },
      { id: 'data_center', name: '数据中心' },
      { id: 'ruined_library', name: '废图书馆' },
      { id: 'network_nexus', name: '网络中枢' },
      { id: 'memory_abyss', name: '记忆深渊' },
      { id: 'lost_village', name: '失语者聚居地' },
    ];
  }

  // 任务列表数据
  _questList() {
    const quests = [];
    const f = this.flags;
    const k = this.karma;
    // 主线
    quests.push({ cat: '主线', text: this.objective.text, done: this.objective.done });
    if (f.met_shuyuan) quests.push({ cat: '主线', text: '找到守砚并获赠刻刀', done: true });
    if (f.sidescroll_knife) quests.push({ cat: '主线', text: '在江堤获得记忆合金小刀', done: true });
    if (f.portal3d_done) quests.push({ cat: '主线', text: '穿过维度裂隙', done: true });
    if (f.stadium_puzzle_solved) quests.push({ cat: '主线', text: '点亮体育馆诗屏', done: true });
    // 支线
    for (const qid of this.completedQuests) quests.push({ cat: '支线', text: `唤醒失语者：${qid}`, done: true });
    // 收集
    const chars = [...new Set(this.player.collectedCharsAll)];
    if (chars.length) quests.push({ cat: '收集', text: `已获汉字碎片：${chars.join('、')}`, done: true });
    // 道德
    quests.push({ cat: '倾向', text: `仁慈 ${k.mercy} · 武力 ${k.violence} · 救助 ${k.saved}`, done: false });
    // 刻字
    if (this.engravings.length) quests.push({ cat: '刻字', text: `已刻字 ${this.engravings.length} 处`, done: true });
    // 第五章进度
    if (this.flags.chapter5_started) {
      quests.push({ cat: '余烬', text: '第五章：余烬已开启', done: true });
      if (this.flags.shard1_done) quests.push({ cat: '余烬', text: '记忆碎片·其一「命名」已获取', done: true });
      if (this.flags.shard2_done) quests.push({ cat: '余烬', text: '记忆碎片·其二「保护」已获取', done: true });
      if (this.flags.shard3_done) quests.push({ cat: '余烬', text: '记忆碎片·其三「完整」已获取', done: true });
      if (this.flags.chapter5_choice) quests.push({ cat: '余烬', text: '终章选择已完成', done: true });
    }
    // 方知远日记收集进度
    if (this.player.diaries && this.player.diaries.size > 0) {
      quests.push({ cat: '收集', text: `方知远的日记 ${this.player.diaries.size}/6${this.flags.all_diaries ? '（已集齐）' : ''}`, done: !!this.flags.all_diaries });
    }
    // 语言种子收集进度
    if (this.player.seeds && this.player.seeds > 0) {
      quests.push({ cat: '收集', text: `语言种子 ${this.player.seeds}/3${this.flags.all_seeds ? '（已集齐）' : ''}`, done: !!this.flags.all_seeds });
    }
    return quests;
  }

  // ============================================
  // 存档系统方法
  // ============================================
  // 快速存档到自动槽
  _quickSave() {
    const ok = autoSave(this);
    if (ok) {
      this._saveFlash = 1500;
      this.showHint('✓ 已自动存档（F5）');
      audio.playSfx('save');
    } else {
      this.showHint('✗ 存档失败（localStorage 不可用）');
    }
  }

  // 快速读档（自动槽）
  _quickLoad() {
    const snap = loadSnapshot('auto');
    if (!snap) {
      this.showHint('没有可读取的自动存档');
      audio.playSfx('uiCancel');
      return;
    }
    audio.playSfx('load');
    fx.transition(600, () => {
      const ok = restore(this, snap);
      if (ok && this._pendingScene) {
        this.loadScene(this._pendingScene, this._pendingSpawn);
        this._pendingScene = null;
        this._pendingSpawn = null;
        this.showHint('✓ 已读取自动存档（F9）');
      }
    });
  }

  // 存档菜单更新
  _updateSaveMenu(dt) {
    const menu = this._saveMenu;
    if (!menu) return;
    // ESC 关闭
    if (input.wasPressed('escape') || input.wasPressed('f6')) {
      this._saveMenu = null;
      audio.playSfx('uiCancel');
      return;
    }
    const saves = listSaves();
    const n = saves.length + 1; // +1 为"新建存档"行
    if (input.wasPressed('arrowup') || input.wasPressed('w'))
      this._saveMenuIdx = (this._saveMenuIdx - 1 + n) % n;
    if (input.wasPressed('arrowdown') || input.wasPressed('s'))
      this._saveMenuIdx = (this._saveMenuIdx + 1) % n;
    if (input.wasPressed('e') || input.wasPressed('enter')) {
      if (this._saveMenuIdx < saves.length) {
        // 读取已有存档
        const snap = saves[this._saveMenuIdx];
        const ok = restore(this, snap);
        if (ok && this._pendingScene) {
          this.loadScene(this._pendingScene, this._pendingSpawn);
          this._pendingScene = null;
          this._pendingSpawn = null;
          this.showHint(`✓ 已读取存档（${snap.slot === 'auto' ? '自动' : '槽位 ' + snap.slot}）`);
          audio.playSfx('load');
        }
        this._saveMenu = null;
      } else {
        // 新建存档到第一个空槽位
        let slot = 0;
        const allSaves = listSaves();
        for (let i = 1; i <= SAVE_SLOTS; i++) {
          if (!allSaves.find(s => s.slot === i)) { slot = i; break; }
        }
        if (slot === 0) slot = 1; // 全满则覆盖槽位1
        saveToSlot(this, slot);
        this.showHint(`✓ 已存档到槽位 ${slot}`);
        audio.playSfx('save');
        this._saveMenu = null;
      }
    }
  }

  // ============================================
  // 体育馆屏幕墙陷阱：靠近屏幕墙时减速 + 持续扣 SAN
  // ============================================
  _updateScreenWallTrap(dt) {
    if (!this.scene || !this.scene.props) {
      this._screenWallSlow = 0;
      this._screenWallSanDrain = 0;
      return;
    }
    // 仅体育馆场景启用屏幕墙陷阱
    if (this.scene.id !== 'stadium') {
      this._screenWallSlow = 0;
      this._screenWallSanDrain = 0;
      return;
    }
    // 检测玩家是否在任意屏幕墙的影响范围内（距墙边 20 像素内）
    let near = false;
    for (const p of this.scene.props) {
      // 屏幕墙的标识：name 包含"屏幕"，或有 type='screen'
      const isScreen = (p.name && String(p.name).includes('屏幕')) || p.type === 'screen';
      if (!isScreen) continue;
      const px = this.player.x, py = this.player.y;
      // 矩形扩展检测
      if (px > p.x - 20 && px < p.x + p.w + 20 &&
          py > p.y - 20 && py < p.y + p.h + 20) {
        near = true;
        break;
      }
    }
    if (near) {
      this._screenWallSlow = 200; // 减速持续 200ms（每帧刷新）
      this._screenWallSanDrain = 200;
      // 持续扣 SAN（每秒 4 点，受难度影响）
      const drain = 4 * (dt / 1000) * difficulty.currentMul().sanDamage;
      this.player.san = Math.max(0, this.player.san - drain);
      // SAN 归零触发死亡
      if (this.player.san <= 0 && !this.combat.dead) {
        this._handleScreenWallDeath();
      }
    } else {
      if (this._screenWallSlow > 0) this._screenWallSlow -= dt;
      if (this._screenWallSanDrain > 0) this._screenWallSanDrain -= dt;
    }
  }

  _handleScreenWallDeath() {
    this.combat.dead = true;
    audio.playSfx('death');
    fx.shake(16, 600);
    fx.flash('#cc4444', 0.5, 400);
    this.player.san = this.player.maxSan;
    const respawn = this._nearestKeystoneSpawn();
    if (respawn) {
      this.showHint('屏幕的噪音吞噬了你的理性……在要石旁醒来。');
      this.loadScene(this.scene.id, respawn);
    } else {
      this.showHint('你跌回入口的微光中……');
      this.loadScene(this.scene.id);
    }
    this.combat.dead = false;
  }

  // 供 player.js 查询：当前是否被屏幕墙减速（返回速度倍率）
  getSpeedMul() {
    return this._screenWallSlow > 0 ? 0.5 : 1.0;
  }

  // ============================================
  // 背包面板（I 键）：查看/使用道具
  // ============================================
  _updateInventoryPanel(dt) {
    const items = this.player.inventory;
    if (!items.length) {
      if (input.wasPressed('e') || input.wasPressed('escape') || input.wasPressed('i')) {
        this.uiPanel = null;
        audio.playSfx('uiCancel');
      }
      return;
    }
    if (!this._invSel) this._invSel = 0;
    this._invSel = Math.min(this._invSel, items.length - 1);
    if (input.wasPressed('arrowup') || input.wasPressed('w'))
      this._invSel = (this._invSel - 1 + items.length) % items.length;
    if (input.wasPressed('arrowdown') || input.wasPressed('s'))
      this._invSel = (this._invSel + 1) % items.length;
    if (input.wasPressed('e') || input.wasPressed('enter')) {
      this._useItem(this._invSel);
    }
    if (input.wasPressed('escape') || input.wasPressed('i')) {
      this.uiPanel = null;
      audio.playSfx('uiCancel');
    }
  }

  // 使用道具
  _useItem(idx) {
    const item = this.player.inventory[idx];
    if (!item) return;
    audio.playSfx('uiConfirm');
    if (item.id === 'old_page' || item.id === 'page') {
      // 旧书页：恢复 30 SAN
      const heal = Math.min(30, this.player.maxSan - this.player.san);
      this.player.san = Math.min(this.player.maxSan, this.player.san + 30);
      this.player.inventory.splice(idx, 1);
      this.showHint(`使用旧书页，理性 +${heal}`);
      fx.flash('#ffd866', 0.3, 300);
    } else if (item.id === 'knife') {
      this.showHint('记忆合金小刀：战斗中用来刻字/攻击，无需消耗。');
    } else if (item.id === 'poem_guanju') {
      this.showHint('《关雎》诗页：已收录，作为武器使用。');
    } else {
      this.showHint(`${item.name || '道具'}：暂无使用效果。`);
    }
  }

  // 获取背包数据（供 render.js 绘制）
  getInventoryData() {
    return {
      items: this.player.inventory.map((it, i) => ({
        ...it,
        selected: i === (this._invSel || 0),
      })),
      san: this.player.san,
      maxSan: this.player.maxSan,
      chars: [...new Set(this.player.collectedCharsAll)],
    };
  }

  // ============================================
  // NPC 游荡行为：失语者小范围随机移动
  // ============================================
  _updateNpcWander(dt) {
    if (!this.scene || !this.scene.interactables) return;
    for (const it of this.scene.interactables) {
      // 仅对 cure（失语者）添加游荡；dialog 类型多为静态物体（告示/轿车/书架等），不游荡
      if (it.type !== 'cure') continue;
      // 已完成治愈的 cure NPC 不再游荡
      if (this.completedQuests.has(it.puzzle)) continue;
      // 初始化游荡状态
      if (it._wander === undefined) {
        it._wander = {
          homeX: it.x, homeY: it.y,   // 原点
          tx: it.x, ty: it.y,          // 目标点
          timer: Math.random() * 3000, // 下次决策计时
          phase: Math.random() * 6,    // 行走动画相位
        };
      }
      const w = it._wander;
      w.timer -= dt;
      if (w.timer <= 0) {
        // 选择新目标点（原点 40 像素半径内）
        const ang = Math.random() * Math.PI * 2;
        const dist = 10 + Math.random() * 30;
        w.tx = w.homeX + Math.cos(ang) * dist;
        w.ty = w.homeY + Math.sin(ang) * dist;
        w.timer = 2000 + Math.random() * 4000; // 停留 2-6 秒
      }
      // 向目标点缓慢移动
      const dx = w.tx - it.x, dy = w.ty - it.y;
      const d = Math.hypot(dx, dy);
      if (d > 1) {
        const sp = 0.3 * (dt / 16);
        it.x += (dx / d) * sp;
        it.y += (dy / d) * sp;
        w.phase += dt * 0.01;
      }
    }
  }
}
